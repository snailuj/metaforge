# Programme Pipeline

The single source of truth for what comes next. Always read this when starting milestone-level work; update it whenever a milestone changes status.

**Reading guidance for agents:** the immediate next job is *always* the first item under **Next**, regardless of whether it's a fresh milestone, a code-review-loop, a tooling task, or any other bridging work. Do not skip ahead to a milestone in Queued just because Next contains a non-milestone item — the file is ordered intentionally.

## Active

- **═══ COMPACTION ANCHOR — metaphor-graph programme state (2026-06-15) ═══** *(self-contained summary of the judge / edge-harvest / generation arc so it survives context compaction; full evidence in the dated `docs/inbox/` files cited)*
  - **THE UNIFYING INSIGHT (2026-06-15):** real metaphor is **multi-word**; our representation is **single-word-WordNet-synset**. That one mismatch is the root of THREE separately-observed symptoms — (1) `bad_head` ≈ phrase IMPOVERISHMENT (`buried wound→wound`, modifier dropped, ~30/44 tagged; `2026-06-14-bad-head-disambiguated.md`); (2) `merge` (operator's corrected def: step C follows only from A+B combined, i.e. a multi-word phrase smuggled across serial single-word nodes, breaking the context-free-hop invariant); (3) the Phase-B vehicle skips (`pressed flower`/`whitespace`/`cloisonné` dropped: "no synset"). **Same disease, three sites.** The high-value structural fix is the **phrase-as-node contract** (deferred architecture brainstorm, now evidence-backed as load-bearing). The "edge is the SEED, not the metaphor" frame (2026-06-13) holds: completion harvests endpoint `(topic_synset→vehicle_synset)` seeds; a runtime **compositor** (fine-tuned-local OR conditioned-SOTA — NOT vapour, it's the forge runtime) realises them with modifiers/register; **Level-3 image→image is scoped OUT** (genre-fiction writers' own craft).
  - **JUDGE (`.worktrees/judge-harness`, branch `metaphor-graph/judge-harness`, NOT merged; 111 tests):** Stage-1 construction **FAILED+CLOSED** (κ 0.13-0.17 both models) — and we now know WHY: `bad_head` is a **polysemous label** (impoverishment / wrong-sense / word-preference / bare-positional), so no judge can learn a coherent function for it. Stage-2 liveness **MARGINAL PASS** (sonnet κ 0.332, band [0.278,0.370], ~70% of the 0.47 human ceiling; kill-gate does NOT fire). UNTUNED (1 model, persona v1, k=6).
  - **⚠️ SENSE-MISMATCH CAVEAT (2026-06-15, operator-found, LARGE ramifications — `2026-06-15-sense-mismatch-contaminates-gold.md`):** the operator sometimes graded a DIFFERENT word-sense than the snapped synset (`fault`=tectonic-not-tennis; `heliotrope`=flower-not-mineral). ⇒ gold is sense-contaminated; **every κ is deflated by sense-noise, not taste** — so BOTH the operator's 0.47 self-consistency AND the judge's 0.332 are likely UNDER-estimates. The judge (always sees the gloss) is MORE sense-disciplined than the gold → unfairly penalised → true judge quality probably > 0.33. Fix ≠ snap-to-dominant-sense (the RARE sense is often the better metaphor); fix = sense-grounded grading + use the judge to flag divergent-sense gold for re-grade. Confirms edge must be synset-keyed (sense is load-bearing for liveness).
  - **PHASE B GENERATION — RUNNING (handpicked-324, AVOID on, tw-floor 0.01):** 2,277+ chains to `data-pipeline/grading/sonnet_chains_provisional_r3_phaseb.jsonl`, auto-resumed cleanly through session limits, within $120 cap. Proxy live-rate 0.0 (emotions read dead by conservative Haiku — expected; real liveness = operator grading). NOT yet deployed to grading-live. Endpoints sense-clean (curated). ~3.5% vehicle-skip (the multi-word-vehicle loss above).
  - **SEQUENCING — REVISED 2026-06-17 (operator's 3-block reorder; SUPERSEDES the earlier 7-step list).** The two judges are renamed onto the operator's two grading axes: **LINKAGE JUDGE** = old Stage-1 construction (FAILED/CLOSED, κ 0.13-0.17) · **LIVENESS JUDGE** = old Stage-2 pairing (MARGINAL PASS κ 0.332, likely an UNDER-estimate per the sense caveat). Order = **Stock Run (background) → Remediation → Phrase-as-Node → First Completion → [Compositor]**. Why phrase-as-node lands BEFORE path-using completion: the only completion signal found is path geometry (`max_hop_cos`), which reads intermediate steps — mis-snapped steps are an uncontrolled variable, so the steps must be sense-clean (the Step-Snap Audit, in Phrase-as-Node) first.
    - **Block 0 — STOCK RUN (background, unattended):** Phase B generation (handpicked-324, AVOID, tw-floor 0.01) → edge-harvest stock. Owns the subscription session; LLM jobs run serial behind it. No deadline urgency on anything else (judge re-measures are single-$ on API anytime). **⇒ The next Stock Run runs in the emit-the-sense shape (BUILT 2026-06-18, see Block 2 item):** every node carries the model's intended gloss and is gloss-matched, so the corpus is born sense-resolved instead of inheriting the measured ~16% snap-back contamination. The decisive reason to land this BEFORE spending the window: a retained phrase is a lemma, not a sense — an old-form run permanently loses the model's generation-time sense intent, which no $0 re-snap can recover. Multi-word phrases stay un-collapsed; harvest carries the per-occurrence sense (sense-SET, per the promiscuity finding). Keep proven knobs (AVOID, tw-floor 0.01, resume wrapper, money brakes).
    - **Block 1 — REMEDIATION (clean Gold + cheap re-measure):**
      - ✅ *DONE* **Gloss Reconciliation / Contamination Survey** — Sonnet subagent flagged WRONG_SENSE/RARE_OK per endpoint; **~5% endpoint contamination** (topics 7.6%, vehicles 4.5%, 11 RARE_OK); root cause = snapper picks the **lowest-id noun synset** → process-nominals ("act of X") / verb senses. Worklist `data-pipeline/grading/sense_flags_provisional.jsonl` (111) + `docs/inbox/2026-06-15-gloss-reconciliation-endpoints.md`. (Snap-Integrity sub-check: grade-time gloss IS the stored synset's WordNet gloss → the divergence is a wrong-SENSE snap, not a display bug.)
      - ✅ *DONE — BUILT + DEPLOYED 2026-06-17* **Sense-Check grading mode** — 4th grade view (`mf-grade-sensecheck`) anchoring the snapper fix to **human sense-gold** (the subagent is an unmeasured oracle → demoted to a pre-sorter; "we eval off you, not the subagent"). Stratified flagged + random-OK sample; Right / Wrong / Rare-but-better / Unsure + candidate-sense picker + chain-context expand + Skip; labels → a SEPARATE `sense_labels_provisional.jsonl` (never the gold judgements). Live on grading-live via the code/data-split cutover — see the **Grading-app reconciliation (round 1)** item in Next.
      - ✅ *DONE — BUILT + RUN 2026-06-18* **Gloss-Reconciliation analysis harness** — offline, off the **121 distinct** human sense-labels (`data-pipeline/scripts/gloss_reconciliation_analysis.py`, 33 tests; report `docs/inbox/2026-06-18-gloss-reconciliation-analysis.{md,json}`; labels live in `.worktrees/grading-data`). **Findings:** (1) **Subagent = recall-oriented pre-sorter, NOT gold** — precision **66%** (a third of its flags are actually apt — mostly poly-apt splits or `right`), recall **90%** on the labelled sample (but the sample over-weights flagged endpoints, so corpus-recall is lower — see silent noise). Confirms "demoted to pre-sorter; we eval off you." (2) **Silent noise is real** — **15.6%** of *unflagged* endpoints are WRONG (Wilson CI 6.9–31.8%, n=32 vehicles+spike-topics), **above** the subagent's 5% self-estimate → it under-flagged; Endpoint Cleanup can't just trust the 111 flags. (3) **Re-snapper floor:** a static dominant-SemCor-tagcount prior recovers an apt sense for **44%** of the 50 wrong snaps for **\$0** (current snap acc 51% → 61%); Gloss-Matched Snapper (Lesk/gloss on top) is the ceiling. (4) **Promiscuity ⇒ sense-SET, measured** — see the Block 2 Gloss-Matched-Snapper decision (now RESOLVED on data, not hunch).
      - ✅ *DONE — GLOSS-BACKFILL 2026-06-19* **Endpoint Cleanup, done the no-remap way** — instead of a deterministic re-snap (which would change `chain_signature` and need a verdict-remap), the **gloss-backfill** (`gloss_backfill.py` + `run_gloss_backfill_loop.sh`) has the model infer an in-context gloss per node on the EXISTING chains, then re-snaps via `snap_by_gloss`. Ran over the whole corpus (spike+curated+stock, **7,515 chains**) → NEW `data-pipeline/output/**/chain-topics_*_glossed.jsonl` (originals untouched). **`chain_signature` preserved → NO verdict-remap needed, all grades/121 labels stay valid** — sidesteps the caveat below entirely. **Measured vs the 121 gold: 52%→73% snap accuracy (+21pp); 26 fixed / 5 regressed (5:1) / 18 lateral apt→apt** (poly-apt churn; vehicles ARE sets). Scalar disambiguation chosen (sense-SET deferred to Phrase-as-Node, non-destructive). ✅ **EMBED RE-SNAP + PROMOTED LIVE 2026-06-20.** Free FastText-embedding re-snap (`snap_by_gloss_embed` + `resnap_glossed_corpus.py`, $0, no re-gloss) lifted snap accuracy on the 121 gold **73%→78%** (fixed `temple`/`wedge`/`probe` token-overlap fumbles; 9 improved / 4 regressed, residual = poly-apt churn). **End-to-end 52%→78% (+26pp).** PROMOTED: the cleaned `*_glossed_embed` corpus replaced the live grading chain files in `.worktrees/grading-data` (atomic in-place swap, canonical filenames — no glob collision; pre-promotion backup on disk + git history). Gloss precompute regenerated over the new synset set (6880 rows, zero gap). **All 132 graded verdicts still resolve** (signature-keyed); sidecar reads per-request → live immediately, NO restart. `grading-data` @ `0402dde9`. The original deterministic-re-snap plan (kept for reference): deterministic endpoint re-snap to clean Gold; fixes the **endpoint-sense bug** (`anger`→sid 30227 verb, `anchor`→sid 23626 verb) + closes the **509-vehicle gloss-coverage gap**. ⚠️ **verdict-remap caveat** (now moot for the backfill path): a synset re-snap changes `chain_signature` → needs a **verdict-remap, NOT a hotfix**. **PRESERVE the 11 RARE_OK**.
      - ✅ *DONE 2026-06-22* **Liveness Re-measure** — re-ran the LIVENESS JUDGE on the sense-cleaned Gold as a clean A/B (old vs cleaned endpoint sense; unified gloss map so neither arm gloss-starved; cleaning changed the **vehicle** sense on 62/127 pairings, topic 0). **κ 0.297 → 0.335 (+0.038)**, acc +0.022, **+6.1pp accuracy on the 62 corrected pairings** (free cache-only attribution; −1.5pp noise on untouched). Direction + mechanism confirmed (`swarm` n→v "move in large numbers") but **modest + underpowered** (bands overlap at n=127). Verdict: sense-cleaning is a net-positive contributor **already promoted live at $0 ongoing**, NOT the unlock — the bigger judge levers stay **more gold (power)** + **prompt/persona tune**. Report: `docs/inbox/2026-06-22-liveness-resnap-remeasure.md` (judge-harness branch).
      - **LINKAGE JUDGE re-tune — PARKED until after Block 2.** Do NOT re-tune now: its `bad_head` label is incoherent (impoverishment / wrong-sense / word-pref / positional) and phrase-as-node rewrites the substrate. BANK the operator's corrected `merge` def + the **reasoned-verdicts-in-few-shot** idea in the template; revisit when the labels are coherent.
    - **Block 2 — PHRASE-AS-NODE (the architecture upgrade; the unifying fix).** Node contract = **phrase + correct sense**. Dissolves `bad_head` + `merge` + vehicle-skip together (one disease, three sites). Brainstorm options: phrase-keyed edges? synset-for-geometry + phrase-for-display hybrid? FastText-vector vehicles when no synset? **✅ CORE BUILT 2026-07-10 (pipeline + grading-tool scope; operator-approved design 2026-07-10):** chain.v2 (phrase-first `node_ref` + per-OCCURRENCE `apt_senses`; verdict asserts the INTENDED sense-path, operator ticks = local co-aptness, sibling paths derived-with-provenance at Block 3) · per-hop noun-prior snapper + vec: admission gate · $0 signature-preserving migration RUN over all 7,515 chains (195/195 gold sigs resolve · 872 steps corrected · 976 vec: steps rescued) · grading tool consumes it (phrase-first labels incl. 3D graph nodes, per-step sense fan → `step_apt_senses` on the verdict, vec: affordance) · judge-harness v2 reader. Spec `docs/superpowers/specs/2026-07-10-phrase-as-node-design.md` · build report + **CUTOVER RUNBOOK (pending operator sudo; live tool untouched until then)** `docs/inbox/2026-07-10-phrase-as-node-build.md`. ⚠️ Wrong-NOUN-sense snaps are NOT fixed at snap time (noun-prior fixes cross-POS only; 1/27 known-bad rows changed) — the sense fan makes them correctable at grading; measure bad_sense ≤10% target on guided-walk batch 4 post-cutover. Remaining in this block: Step-Snap Audit (per-sense edge enumeration) + linkage-judge re-tune (both below). Contains:
      - **Head-extraction remediation** — `bad_head` is **phrase IMPOVERISHMENT, NOT syntactic**; the deterministic `head_extractor.py` (built, 28 tests) fixes 32 *untagged* corpus chains but **0 of the 44 tagged** → it doesn't touch the tagged class. The real lever is the **node contract**, not a parser or a re-snap (`docs/inbox/2026-06-14-bad-head-{disambiguated,is-not-syntactic}.md`).
      - **Step-Snap Audit** — Gloss Reconciliation run on intermediate STEPS (not endpoints), human-anchored. The operator's hard prerequisite that steps be sense-verified before Path Completion. Lives HERE (not Block 1) because the node contract decides how a step is represented/snapped — auditing bare-head snaps the new contract will discard is wasted motion. Also triggers **per-sense edge SPLITTING**: for TWO reasons (operator-found, sense-check 2026-06-18): **(a) conflation/error** — one `(word, synset_id)` carries *divergent* intended senses across its chains (the single-synset-node problem at the *sense* level); **(b) poly-aptness/opportunity** — a single `topic→vehicle` pairing is genuinely LIVE across *several* senses even from ONE chain (`whisper→glance` works as glance-the-look (n), glance-briefly (v), AND glance-off-at-an-angle (v) — three live sense-edges from one chain). Either way the endpoint must become **multiple sense-keyed edges**, not one verdict; (b) is **generative richness** (more apt edges from the same pairing — the forge-not-index thesis), not a disambiguation chore. A `split` verdict flags these (currently a flag only; the per-sense enumeration happens in this pass).
      - **Gloss-Matched Snapper (deterministic)** — SemCor `sense_attributes.tagcount` dominant-sense prior + WordNet `domains`/`domainid` type + gloss-overlap/Lesk WSD over the **retained `chain[i].phrase`**. Fixes the snap-BACK of the model's free-text output (`resolve_synset` currently picks one synset per bare lemma with no prior, behaviourally lowest-id). Existing corpus = **pure re-snap, NO regeneration** (phrases retained; recovers ≥12/21 topics + most vehicles). Synset IDs are byte-offsets (numeric nearness ≠ meaning — do WSD, not arithmetic). **Evaluated vs the HUMAN sense-labels, never the subagent. PRESERVE RARE_OK.** Vehicles get a **concrete/noun-sense PRIOR** — POS divergence from a (typically nominal) topic is a wrong-sense *signal* (vehicles skew concrete nouns: `wedge`-the-tool, not `to wedge`), a PRIOR not a hard POS-parity gate, so valid cross-POS images (`anger→simmer`, `deadline→loom`) still win (operator-found 2026-06-18). **⚠️ POSSIBLE PIVOT (hypothesis, operator-observed 2026-06-18, GATED on measurement):** if MANY senses are often apt per node (metaphor/prose is sense-*forgiving* — operator using Split heavily, sometimes selecting MOST senses: "they genuinely could be made to fit"), the snapper is the wrong *shape* — it should model a **sense-SET** (or a word/phrase node with sense as a generative knob the **compositor** resolves at realisation), not disambiguate to one synset. This SOFTENS the wrong-sense contamination (much 'wrong' is really a different-but-still-apt sense; the live signal moves UP to the **word-pair**, not the synset) and doubly-justifies word/phrase-as-node (a synset collapses the phrase AND the word's sense-fertility). **Caveat — it's a SPECTRUM, not 'everything fits':** the operator still marks genuine wrongs (some senses are dead — the verb `wedge`); collapsing to all-apt makes aptness meaningless. **✅ DECISION RESOLVED ON DATA (2026-06-18 harness, 121 labels) → sense-SET, with a single-sense cleanup interim.** The promiscuity measure: **split 44.5%** of determinate labels, **mean 3.18 apt senses per split**, and the operator marks **66.6%** of a lemma's WordNet senses apt when he splits. That decisively favours the **sense-SET model** (or word/phrase node + sense-as-generative-knob the compositor resolves) over a single-sense classifier — collapsing to one synset throws away ~⅔ of the live sense-fertility. BUT the spectrum caveat holds (~37% determinate are outright `wrong` — dead senses are real), so the **Gloss-Matched Snapper still ships as a single-sense Endpoint-Cleanup interim** (kills the egregious process-nominal/verb snaps — the dominant-tagcount prior alone fixes 44% of them) while the sense-SET representation is the Phrase-as-Node endgame. The two are complementary, not contradictory: cleanup removes dead-sense snaps; the set captures live multiplicity. **⚠️ Drift confound CONFIRMED:** split rate rose **+25.3pp** (31.6%→56.9%) first-half→second-half as the operator calibrated → early `wrong`s are under-counted splits, so the true promiscuity is **higher** than 44.5% and the wrong-rate (41) is inflated. Before treating any early `wrong` as gold for the snapper, **recency-weight or re-pass the first-half labels** (the harness's `drift()` quantifies this; a re-grade of the ~57 first-half endpoints is the cheap fix).
      - ✅ *DONE — BUILT + VALIDATED 2026-06-18* **emit-the-sense** (branch `generation/emit-the-sense`) — the generating model now emits a one-line `gloss` per step (`{phrase, head, gloss}`); the snap-back uses `metaphor_graph.snap_by_gloss` (Lesk gloss-overlap WSD over the lemma's candidate senses, fallback to the legacy lowest-id resolver on zero evidence) instead of the no-prior pick that caused the contamination. **Additive, NOT a restart**: optional `ChainStep.gloss` field (all old records still validate); `chain_records_from_sonnet(resolve_by_gloss=…)` defaults to byte-identical prior behaviour; **automatic on the next Stock Run** (`generate_metaphor_edges.main` wires `make_gloss_resolver(conn)`; needs `--db`, which the run already passes — no new flags, no wrapper change). **Real-data validation:** 5/5 of the report's named contamination cases recover the correct sense (`hush`→tranquil-silence not hydraulic-wash; `chisel`→edge-tool not swindle; `glamour`→charm not cast-a-spell; `tension`→emotional-strain not stretching; `murmur`→low-sound not phonetic-schwa). TDD: ChainStep field + `snap_by_gloss` (5 tests, fixture DB) + prompt + ingester (gloss-match/fallback/uniform-gloss-key) + runner wiring. Cost: ~one short line × nodes × vehicles per topic (within the existing money brakes); malformed-gloss falls back gracefully (format-drift tripwire still guards JSON).
      - then revisit the **LINKAGE JUDGE re-tune** on the now-coherent labels.
    - **Block 3 — FIRST COMPLETION:** judge-harvest the ~2,750 chains into endpoint edges (LIVENESS JUDGE, ~$5-10) → run completion. **Endpoint Completion** (clean `topic_synset→vehicle_synset` seeds) could run early; **Path Completion** (the `max_hop_cos` signal) MUST wait for clean steps (Block 2). The operator optimises for Path Completion ⇒ completion-last is correct. **⇒ STARTED 2026-07-10 — FIRST SIGNAL IS POSITIVE:** harvest r1 banked 876 provenance-tagged edges (curated cohort, v6 judge, $15 cap; **OpenRouter credits EXHAUSTED mid-run — top-up needed before any judge work resumes**; live-rate 51.9%, v6 false-live lean caveat). Learning curve on the 864 synset-resolved edges (topic-grouped 20×5 CV): **full cascade-feature model AUC 0.695 [0.619, 0.765]** vs cascade_only 0.589 vs chance — the June "features at chance" null reverses at n=864 (power problem). **Both curves FLAT → the lever is FEATURE expressiveness (path geometry via Step-Snap Audit + fan ticks), not more labels.** Caveat: this is completion-of-the-JUDGE (features →0.70→ judge →κ0.52→ operator). Report: `docs/inbox/2026-07-10-block3-endpoint-completion-first-signal.md` (judge-harness branch).
    - **Downstream (decoupled):** the **Compositor** (forge runtime: renders a harvested seed with modifiers/register; fine-tuned-local OR conditioned-SOTA OR hybrid; **Level-3 image→image scoped OUT** = genre-fiction writers' own craft); **Taste-validity panel** (writing class, inter-rater κ vs gold — cheapest insurance on "κ measures one curator"). **Parked secondary levers:** SemCor re-snap for the ~15 unflagged wrong-sense INTERMEDIATES (NOT the bad_head lever); batch-infra consolidation (`batchkit`).

- **Judge Agreement Harness + Two-Stage Judge — Phase A: BUILT + CALIBRATED (first pass)** *(plan: [`docs/plans/2026-06-10-judge-harness-plan.md`](../plans/2026-06-10-judge-harness-plan.md); built on `.worktrees/judge-harness`, branch `metaphor-graph/judge-harness`, NOT merged)* — 5-module harness (corpus/harness/llm-cache/stage1/stage2) + checkpointed-JSONL/heartbeat/auto-resume instrumentation, 111 tests green. Leave-one-topic-out κ, content-addressed call cache, session-limit parse-and-wait. Calibration results (2026-06-12/13, sonnet+haiku, evidence in `docs/inbox/2026-06-12-stage{1,2}-calibration*.{md,json}`):
  - **Stage-1 construction (linkage gate): FAILED + CLOSED.** haiku κ 0.169, sonnet κ 0.131 (gate 0.4). Bad-linkage not judgeable from chain text by two model classes; mechanical wrong-head detector 0/44 recall; generation-side repair only halved bad_head (54%→26-33% by cohort). **Held pending the head-extractor fix — do not prompt-tune past it.**
  - **Stage-2 liveness (pairing only): MARGINAL PASS.** sonnet κ **0.332**, band [0.278, 0.370], acc 0.667 vs 0.566 majority (5 repeats, n=565). **Kill-gate does NOT fire** (band well clear of 0; pooled ≥ 0.3 target). A *fair*, untuned judge capturing ~70% of the 0.47 human ceiling — real headroom in k-sweep / persona iteration / model choice, none yet done. Vindicates orthogonal design: the gate that matters for a pairing-corpus passed; the chain-internal gate (Stage-1) is the one that's unjudgeable.
  - **Operator due-diligence (open):** prompt templates rendered for review (`docs/inbox/2026-06-12-judge-prompt-templates.md`); operator may revise persona/rubric before any judge re-tune (he is the taste source). Open question he raised: κ measures fit-to-one-curator's-taste; widen via a small external rater panel (his writing class = ready-made) computing inter-rater κ vs gold, and eventually product telemetry (kept metaphors) as the population signal.
  - **Funding window:** all judge tuning is single-digit-$ on the API **anytime** (per `docs/inbox/2026-06-12-api-economics.md`) — NO June-15 urgency. The ONLY window-urgent item is Phase B generation. LLM jobs run SERIAL (CLI session caps).
  - **Conceptual reconciliation (2026-06-13):** "judged pairing" ≡ "harvested apt-metaphor graph edge" `(topic_synset→vehicle_synset, live/dead, tiers[])` = a `metaphor_bridges` row. The forge-not-index corpus is the *edge-harvest substrate* for the operator's completion/distillation hypothesis, NOT a serving index (index *serves* edges = rusts; forge *learns from* them = supervision, never ships). Intermediate paths demoted to candidate-features (topology doesn't recur, geometry deflates) but all stored; re-elevatable if bare-edge completion stalls.

## Next

- **TWO-TRACK PLAN — declared 2026-07-02 after the farm-out bake-off verdict** *(spike report: [`docs/inbox/2026-07-02-farmout-model-bakeoff.md`](../inbox/2026-07-02-farmout-model-bakeoff.md))* — the bake-off settled generation sourcing definitively: **no cheap drop-in preserves Sonnet's liveness** (six arms, per-chain calibrated judge: Sonnet 0.224 > Haiku 0.129 > Flash/GLM ~0.09; the deficit is the chain-EXPANSION stage, hybrids don't recover it). The cheap path (Flash + overgenerate + judge-filter, ~30× cheaper for equal live yield) is viable ONLY with a good cheap live/dead judge — independently re-confirming the judge as the binding constraint (same conclusion as the context-free-edges refutation). Operator directive: run both tracks in parallel.
  - **Track A — LIVENESS JUDGE tune + grading scale.** NOT gated on phrase-as-node (endpoint edges are already sense-clean). Tune the marginal-pass Stage-2 judge — κ 0.332 untuned, ~70% of the 0.47 human ceiling; the untouched levers are k-sweep / persona iteration / model choice (single-$ on API anytime). **Gate 1 CLEARED 2026-07-03** — operator review enacted on `metaphor-graph/judge-harness` (`d2e7dbb9..83425625`): merge definition corrected, fault tags rendered on worked examples, Forge Reader preamble v2, **3 sense-suspect gold rows quarantined** (`gold_sense_suspect.jsonl`: fault/heliotrope/flight — graded under a different sense than the recorded synset), post-branch-split data paths fixed, and **gold vintage floor `--gold-since 2026-06-11`** (operator invalidated all pre-final-form gradings; four grading eras derived from notes-chronology × tool git history, step-indexed "final form" begins Jun-11 12:48). Post-floor gold = **48 rows / 10 topics (47 liveness, 28 live)**; κ 0.332/0.335 are pre-revision numbers, no longer comparable. **Critical path is now GRADING SCALE:** the 83 invalidated rows form a self-healing re-grade queue (a re-grade stamps a fresh ts → row re-enters gold automatically) + the grading-tool triage/filter work (Inbox: "grading UI — filters + triage-score integration"). Re-baseline κ on floored gold (directional only at n=48), sweep once n recovers. Success = judge κ materially up → endpoint edge-harvest (Block 3) unblocked → Flash-overgenerate economics validated or killed.
  - **Track B — PHRASE-AS-NODE (Active Block 2).** The architecture upgrade: node contract = **phrase + sense-set**. Starts with the deferred **brainstorm** (phrase-keyed edges? synset-for-geometry + phrase-for-display hybrid? FastText-vector vehicles when no synset?) then the Block 2 list: Gloss-Matched Snapper, Step-Snap Audit, per-sense edge splitting, then the LINKAGE-judge re-tune on coherent labels. Fixes the one-disease-three-sites cluster (bad_head / merge / vehicle-skip — the bake-off's Flash/GLM runs hit the same ~3.5% "no synset" vehicle loss).

- **Grading frontend unification — editor mode on the real forge surface** *(FAST-FOLLOWER from round 1; 2026-06-17)* — make `editor` mode render via the forge's ACTUAL product components (the metaphor/graph display a user sees), with verdict / candidate-picker / tier affordances as an overlay, absorbing the bespoke `mf-grade-panel` / `-walk` / `-regrade` / `-sensecheck` renderers. Closes the dogfooding gap — today the operator grades against a grading-specific view, so verdicts aren't formed on the product surface. The shell is already mode-based (`mf-app` grade/browse); this pushes integration down to the rendering layer. Promote to the top of Next when round 1 lands.

- **Mode-collapse audit of the existing ~2,750 chains** *($0, pure analysis — no LLM)* — vehicle-reuse distribution, per-topic vehicle overlap, proposer diversity. The quantified half of the "finite Sonnet extraction" fear, and its findings feed the AVOID/diversity steering repair that Phase B's chain prompt needs. Run before (or in parallel with) the judge calibration; report to `docs/inbox/`.
- **Phase B — generation run: LAUNCHED 2026-06-14** *(operator go; running unattended via `run_generation_loop.sh`, PID-tracked, NTFY-alerted)* — pool = **handpicked-324** (sense-clean emotional concepts, `data-pipeline/generation_topics_handpicked.json`) → `data-pipeline/grading/sonnet_chains_provisional_r3_phaseb.jsonl`; AVOID nudge ON (16 over-used+dead-leaning vehicles, `phase_b_avoid_vehicles.json`); `--tw-abs-floor 0.01` (emotions are the audit's low-live-yield class — we WANT the dead examples too, so the collapse floor is dropped); caps `--max-topics 350 --max-cost-usd 120` (≈$80 expected). Live Haiku+Sonnet (AVOID hits both prompts). NOT yet deployed to grading-live (round file must be copied to `.worktrees/next/.../grading/` to be gradeable). Heads backfillable post-hoc (`head_extraction_backfill.py --resnap-file`) — bad_head does NOT gate this. **Extension path:** a quality common-noun 1k needs a ~$5-8 disambiguation pass (serial — wait for this run to pause/finish). Original prep/decision detail below.
  - *(original)* — top ~1k topics ranked by BNC/SemCor demand (tables imported), repaired AVOID/diversity steering (33-vehicle payload from the mode-collapse audit, `docs/inbox/2026-06-12-mode-collapse-audit.md`), existing resumable runner (`run_generation_loop.sh`). Purpose: expand the **edge-harvest substrate** (more topic-vehicle candidate edges for the completion hypothesis), with the Stage-2 judge triaging the output to prioritise the operator's grading attention. **Gate note (corrected 2026-06-13):** the gate was re-pointed from Stage-1 (failed, but it only judges chain-internal construction the corpus doesn't ship) to **Stage-2** (passed — it judges the pairings that ARE the corpus). **Prep before launch (tight in a 2-day window):** (a) wire the AVOID payload into the chain prompt (small TDD task, no spend); (b) select+disambiguate ~1k fresh topics (~$5-8, few hrs, SemCor tagcount path); (c) launch the multi-day run. Decision is the operator's — it commits finite subscription quota and sets direction. If NOT run in-window: $50-150 later on the API (batched −50%), no other loss.

- **M04 tech debt block — COMPLETE — all 3 items landed before M05** *(closed 2026-05-23)*:
  - **(1)** ✓ Structural debt sweep merged to `main` at `a0b488aa` — D24 god-function refactor + D1/D11/D26 type discipline + D15/D19/D20/D23 observability bundle + 4 challenger-rejected refactors (NewCascadeCandidate options struct, return-tuple counter, explicit pipeline deps, phaseOutcome typed enum). 18 items resolved across the upgraded review-loop. catch-fixing-forwards' fix-forward warning on `handler.go` is now closed.
  - **(2)** ✓ `code-review-loop` skill upgrade landed in `claude-config` repo at `8600d45a` — adversarial deferral enforcement: fresh challenger subagent dispatched per `defer-out-of-scope` decision, line-call→fix-now bias, substantive Pass-4 concur criteria (proposed_followup engagement + fix_now_cost_estimate + cost_comparison), sub-1h fix-now auto-rejects deferral, 2-round-survival re-justification. **TDD-verified** in this session: 4-scenario RED-GREEN pass demonstrated the upgrade materially closes the rubber-stamp failure mode (A1 baseline: 3/3 thin concurs; A2 with upgrade: 1 substantive challenge + 2 substantive concurs). All 4 deferral challengers in the M04 sweep review-loop returned `reject_defer` — items the orchestrator would have rubber-stamped were re-triaged as fixes.
  - **(3)** ✓ Lakoff cross-domain eval cohort + sweep run on `m04v2/lakoff-cohort` branch — 80 apt + 90 inapt pairs covering ~30 Lakoff & Johnson 1980 metaphor classes. Sweep [verdict](../../data-pipeline/sweeps/m04v2_lakoff_band_verdict.md): cosine band alone is insufficient; cross-domain pairs surface as candidates but don't beat within-domain neighbours at the scoring step. **Direct evidence that M05's type-aligned scoring is required.** `ModeCluster` remains production default; M04 cosine path ships behind env/flag.

- **M04 tech debt block — COMPULSORY before any M05 work begins** *(declared 2026-05-23 after M04 v1 review-loop critique)* — three-part block. The user flagged the M04 review-loop was being hijacked by excessive deferral argumentation, with deferrals starting to abuse the "carry forward" mechanism for tech debt that goes well beyond YAGNI. All three items below must land before the M05 milestone may start.
  - **(1) Structural debt sweep on `m04/cosine-candidate-gen` branch** *(in-flight 2026-05-23, before merge)*:
    - **D24** — `handleSuggestCascade` god-function refactor into `cascadePipeline` type with `fetch / score / respond` methods. Catch-fixing-forwards fired on this — `handler.go` was modified in all 3 M04 review rounds AND in 3+ prior M03 review logs. Continuing to patch the function compounds the rot. **Binding constraint:** must land before merge.
    - **D1** — `forge.NewCascadeCandidate` constructor that panics on `!source.Valid()`. Round 3 found the D4 fix (embedding tier omission) NOW DEPENDS on the unenforced invariant. Shipping load-bearing logic on a contract nothing enforces is not YAGNI.
    - **D11** — rename `CandidateSources` → `CandidateMode` (config-side) and constants `SourcesCluster`/`SourcesEmbedding`/`SourcesUnion` → `ModeCluster`/`ModeEmbedding`/`ModeUnion`. The singular/plural distinction with `CandidateSource` (per-row) is visually fragile; the compile-time safety claim is partial (explicit casts compile).
    - **D26** — `main.go:54` `forge.CandidateSources(*candidateSources)` is an unguarded type cast. Add `forge.ParseCandidateMode(s string) (CandidateMode, error)` and use it at the cast site so bad env values fail at the boundary, not via downstream `Validate()`.
    - **D15 + D19 + D20 + D23 (observability bundle)** — lift `cascadeAnomalies` to a named type with `Attrs() []slog.Attr` method; add `embeddingPathUnavailable bool` (D15) + `embeddingDimMismatches int` (D19); add Error log when `resolveLemmaSiblingSynsets` returns empty after `resolvePrimaryCuratedSynset` succeeded (D20); use the `.Attrs()` helper to DRY the 6 paired `stopTotal` + `slog.Info` emit sites (D23 + STD-R2-4 from review log).
  - **(2) `code-review-loop` skill upgrade — adversarial deferral enforcement** *(post-merge, before M05 starts)* — the skill currently lets the orchestrator accept "concur" verdicts from Pass-4 reviewers with one-sentence rationales that don't engage with the proposed_followup or test the fix-now alternative. Upgrade the skill to:
    - Require every `defer-out-of-scope` triage decision to be backed by a *fresh adversarial subagent* dispatched specifically to argue FOR fix-now (independent of the reviewer who raised the item).
    - Set the bias: when a deferral is a line-call between "fix now" and "defer", the orchestrator MUST choose "fix now". The burden of proof sits with the deferral.
    - Tighten the `concur` criteria in the adversarial review block: a concur verdict requires the reviewer to engage with the proposed_followup AND explicitly check whether the fix-now cost is small (`<1h work`) — if yes, the deferral fails.
    - Add a structural backstop: any deferral that survives 2 rounds must be re-justified against the current code state, not the state at the time of original deferral.
    - Files to touch: `~/.claude/skills/code-review-loop/SKILL.md`, `references/deferrals.md`, `adversarial-review-prompt-block.md`, the four `adapter-*.md` files. New file: `adversarial-deferral-prompt-block.md`.
  - **(3) Lakoff cross-domain eval cohort + sweep re-run** *(post-merge, before M05 starts)* — the M04 v1 sweep proved nothing because MUNCH is paraphrase-style metaphors (`approach`→`direction`), not the cross-domain pairs M04 targets. Build a Lakoff-classics cohort:
    - Source: Lakoff & Johnson 1980 (Metaphors We Live By) primary cross-domain mappings — anger→fire, idea→light, time→money, argument→war, life→journey, theory→building, love→journey, mind→machine, ideas→objects, understanding→seeing, important→big, etc.
    - Format: same JSONL shape as `munch_apt.jsonl` / `munch_inapt.jsonl` so the existing `m04_sweep_runner.py` driver works unchanged.
    - Plausibility-rated inapt controls: cross-domain pairs that ARE in the embedding band but humans rate as NOT apt (e.g. anger→bunting — the smoke test caught this surfacing in union mode).
    - Re-run the M04 sweep against this cohort to actually measure the generation-broadening lift M04 v1 claims to deliver.
    - Files to create: `data-pipeline/fixtures/lakoff_apt.jsonl`, `data-pipeline/fixtures/lakoff_inapt.jsonl`, a new sweep YAML pointed at them.
  - **Why "compulsory"**: opening M05 type-aligned scoring on top of (a) a 290-line god-function, (b) load-bearing logic that depends on an unenforced type invariant, (c) a review-loop tool that rubber-stamps deferrals, and (d) an eval cohort that can't measure the change being evaluated — would compound every problem M04 surfaced. M05 must start from a clean structural baseline.

- **M04 v2 — Cross-domain eval cohort + sweep re-run** *(promoted 2026-05-23 on M04 v1 sweep close — now subsumed under item (3) of the compulsory M04 tech debt block above)* — the M04 v1 calibration sweep over MUNCH (200 apt + 200 inapt subsample) found all 9 `(d_min, d_max)` cells identical to baseline (`separation_score=0.0258, aptness_rate=0.3333`). Root cause: MUNCH is paraphrase-style metaphors (`approach`→`direction`) — not the cross-domain pairs M04 is designed to surface. 97% of MUNCH pairs don't resolve at all through the API at `limit=50`. The cosine-band path IS working (canary `TestCascadeUnion_ClassicalPairsSurface_AsCandidates` pins anger→fire / idea→light / time→money / truth→hammer surfacing as candidates), but MUNCH simply cannot measure that surface. **v2 deliverable:** construct a Lakoff-classics cohort (cross-domain pairs + plausibility-rated inapt controls) and re-run the sweep harness against THAT. Sweep YAML + driver from M04 v1 are reusable; only the fixture changes. Verdict: [`m04_embedding_band_verdict.md`](../../data-pipeline/sweeps/m04_embedding_band_verdict.md).
  - Operator decision (recorded in verdict): keep `SourcesCluster` as production default; users opt into union/embedding via `METAFORGE_FORGE_CANDIDATES` env var or `--candidate-sources` CLI flag.
  - **M04 v1 shipped value:** binary generation lift confirmed (canary test); CLI/env knobs wired; observability counters (cluster vs embedding source mix, anomaly aggregator, unconditional cascade outcome logging); type discipline tightened (CandidateSource / CandidateSources / Composition.Valid + extended CascadeConfig.Validate); deterministic TopK (stable sort); sibling-sense exclusion; latency hot-path optimised (precomputed topic norm).
  - **Active M04 deferrals (24 — see `docs/superpowers/review-logs/2026-05-23-m04-cosine-candidate-gen-review.md`):**
    - **D5** — cluster-wins-on-conflict drops embedding-path topic synset; semantic correctness gap → M05 type-aligned scoring or `SourceBoth`-keyed dual-distance.
    - **D14** — production p99 latency budget (~500ms target) — Fix D shipped, re-measure under v2 sweep cohort.
    - **D24** — `handleSuggestCascade` god-function refactor into `cascadePipeline` type; flagged by catch-fixing-forwards (handler.go touched in 3+ review runs). **Binding pre-M05 constraint.**
    - **D1 / D2 / D11 / D26** — Source-tag constructor discipline + `CandidateSources` naming sweep; bundle as one "type-discipline" commit before M05.
    - **D3 / D17** — `ForgeEmbeddingConfig` consolidation + `EmbeddingTopK` naming + `getSynsetRow` polysemy-ASC comment.
    - **D9 / D10 / D13** — DB error-handling hygiene sweep (`errors.Is`, SQLite version probe).
    - **D15 / D19 / D20 / D23** — observability gap closures (`embedding_path_unavailable` flag, dim-mismatch counter, sibling-resolver empty log, lift `cascadeAnomalies` to named type).
    - **D6** — multi-sense ANN candidate generation (M04 follow-up — see legacy entry below).
    - **D7 / D8 / D18 / D21 / D22 / D25 / D27** — minor doc/test/observability lifts; bundle.
    - Full ledger at `docs/superpowers/review-logs/2026-05-23-m04-cosine-candidate-gen-review.md` (16 active + 3 superseded).

- **M04 v1 — Cosine-Sim Candidate Generation** *(promoted to Next 2026-05-21; v1 implementation complete 2026-05-23 on `m04/cosine-candidate-gen`)* — original M04 description below for archival reference. v1 ships the cosine-band candidate generator unioning with cluster-overlap path; the eval-cohort question is what v2 must answer.
  - Why before type-alignment: optimising the *scoring* of a candidate set that systematically excludes apt cross-domain pairs is peak diminishing returns. Broadening the set first delivers the eval-cohort lift; M05 type-alignment then sharpens that richer set.
  - Detail doc: [`M04-cosine-candidate-gen-roadmap.md`](M04-cosine-candidate-gen-roadmap.md)
  - Depends on: M03 (done)
  - **Deferrals to address as part of M04** *(carried from M03-S05 review log, 2026-05-21, plus M03-pre-M04-deferrals branch close, 2026-05-22)*:
    - **D9** — cascade ordering by salience+contrast vs final_score; revisit ordering criterion once ANN candidates UNION with cluster-overlap.
    - **D15** — IN-clause batch chunking for `GetSynsetClusterPropertiesBatch`; chunk when the candidate set broadens beyond ~1k.
    - **D17** — formalise the centroid-coverage contract and asymmetric centroid-vs-concreteness lookup discipline in the handler.
    - **R2-D1** — `handleSuggestLegacy` silently degrades to `domainDist=0` when `sourceEmb` absent (legacy path's (nil, nil) benign-absence contract leaves no breadcrumb at request time). Land when cascade becomes default — legacy path may be retired or held to cascade's domainDist-signal discipline.
    - **R1-D7** — `cascade_cache_load_*` timing records emit before the "Metaforge API starting" banner. Cosmetic log-ordering fix; folds into M04's ANN-index startup wiring in `cmd/metaforge/main.go`.
  - **Cascade-anomaly aggregator** *(new slice, queued for M04 or shortly after)*:
    - **R1-D4** — `handleSuggestCascade` logs `Error` + continues when batch `propsByID` is empty for all candidates (200 indistinguishable from legitimate no-gate-pass). Wants a runtime row-count tripwire for `synset_properties_curated`.
    - **R4-D1** — `handler.go:325-328` cache-divergence `Error` log fires per candidate with no per-request cap (asymmetric with the `malformedLogCap` throttle in `db.go`).
    - Both want the same per-request anomaly aggregator: consolidate per-candidate Error logs into a count tagged on the `cascade_request_total` timing record, plus a startup-style runtime tripwire on cascade-supporting tables.
  - **Observability prerequisite** *(landed on `m03/pre-m04-deferrals` 2026-05-22)*: cascade hot-path timing + per-request trace + encode-stage outcome attribution + malformed-blob log throttle — latency baseline now collectable before M04 broadens the candidate pool. M04 should re-run the timing-enabled smoke set after wiring ANN candidates to compare against the M03-S05 baseline.

- **M03 — Cascade Gate-and-Rank** *(Stages 1 + 2 complete 2026-05-20)* — concreteness gate → Ortony rank → domain-distance re-rank. Restructures the pipeline from pointwise formula choice (M02 territory) to structural primitives. Wires in concreteness prediction (already available via `synset_concreteness`) and domain-distance re-rank.
  - Why now: M02 — Asymmetric Ortony Scoring closed empirically negative on 2026-05-16. Every variant in the pointwise-property-overlap family (symmetric, asymmetric, null) landed within ±0.06 of zero separation on a balanced cohort. The pointwise approach is exhausted; structural primitives are the next available lever.
  - **Inherits from M02's retro work**:
    - Trustworthy eval harness on a balanced cohort (random_uniform = +0.0068 ≈ 0, apt 271 / inapt 978, 67% MUNCH retention vs 22% before)
    - Haiku+sensorimotor enrichment: 5.4 sensorimotor properties per synset average
    - S04-A/B's cohort-shape diagnostic methodology — should be the first thing M03 runs before trusting any new verdict
  - **Three-tier eval strategy** (set during 2026-05-16 planning):
    1. **Composed harness** — run apt/inapt pairs through `gate → rank → re-rank`, score the cascade as one fn; cohort-shape preflight mandatory.
    2. **Ablation** — pipeline-only / +gate / +rank / +re-rank; each layer must lift separation without lifting `random_uniform`.
    3. **Lakoff-prediction tests** — independent of harness: do MUNCH-apt pairs show target-more-concrete-than-source asymmetry (pure Brysbaert)? Do apt pairs cluster at intermediate domain-distance?
    - Go/no-go: Tier 2 ablation clean + Tier 3 predictions hold → Tier 1 separation >0.05 is trustworthy lift.
  - **Strategic loop-back** *(noted 2026-05-16)*: after M03 + The Bridge ship, revisit the forge algorithm with the new tools (concreteness gate, domain-distance ranking, Bridge-generated novel-apt cohort). The Bridge cohort especially addresses the novelty/cliché blind spot that neither MRR nor `separation_score` measures.
  - **Two-stage eval baked into the plan** *(added 2026-05-17)*: Stage 1 runs M03 against `lexicon_v2.db.pre-purge-20260517` (the byte-state on which M02's published plateau was measured) — clean ablation against M02's numbers. Stage 2 runs against the rebuilt DB after the 2026-05-17 curated-vocab haiku-sm enrichment lands — forward validation in production data state. Without Stage 1, a single-stage measurement on the rebuilt DB would entangle algorithm + prompt + model + coverage in one delta. The pre-purge backup is therefore load-bearing for M03 and must survive any backup-pruning chore.
  - Depends on: M02 integration merged to main
  - Detail doc: [`M03-cascade-gate-and-rank-roadmap.md`](M03-cascade-gate-and-rank-roadmap.md) *(drafted 2026-05-17)*
  - Branch: TBD (cut from fresh main after M02 PR merges)

## Queued

- **M06 — Novelty Tracking** *(renumbered from M05 on 2026-05-21; optional for MVP, valuable for Substack narrative)* — MuseScorer-style dynamic buckets, creative yield curve dashboard metric. Additive measurement layer.
  - Depends on: M03
- **The Bridge** *(new feature, surfaced during M02-S04 close on 2026-05-16)* — dual of the Forge: given source AND target, return the path through wordspace linking them. Graph search rather than ranking; different mechanism class than pointwise scoring. Two product values:
  - **Explanatory:** "anger → fire" returns the conceptual chain (e.g. `anger → heat → consuming → destruction → fire`), surfacing the metaphor's mechanism for users
  - **Inapt cohort generation:** weak/no-path queries can semi-supervisedly produce inapt examples, expanding the eval cohort beyond MUNCH
  - Algorithmic notes: branching factor ~78/hop, mitigated via salience-weighted edges, bidirectional BFS, embedding-prefilter A*, concreteness gradient, and a precomputed cluster-cluster adjacency matrix. 2-3 hops covers most apt metaphors.
  - Architectural framing *(added 2026-05-21)*: Forge and Bridge share the same language structure — concept-senses as nodes, semantic relations as edges, concreteness gradient, type-aware features. They differ at the *traversal* layer (1-hop frontier vs bidirectional A*), not the substrate. Extract the shared `metaphor` package (graph + cascade + candidate gens) before building the Bridge; both orchestrators then sit on top cleanly. See M04 roadmap doc for the structure-vs-orchestrator argument.
  - Dependency on M04: M04's ANN index over `synset_centroids` IS the Bridge's embedding-prefilter A* layer. Building M04 first reduces the Bridge from "2 days from scratch" to ~1.5 days of orchestration on top of shared infrastructure.
  - Cost: ~2 days to shippable demo if built before M04; ~1.5 days if built after.

## Inbox (untriaged captures)

Zero-friction landing zone for raw ideas/observations captured mid-work — **no triage required at capture time**. This is the single capture lane (supersedes the old `docs/inbox/captures.md`). Triage periodically: promote each item down into **Backlog** (or **Queued**/**Next** if a slot is obvious), move it to `docs/decisions/log.md` if it's actually a decision, raise a GitHub issue only if it's external-facing (licensing, public infra, community), or discard. On promotion, move the text **verbatim** and delete the inbox line. Keep entries short; one bullet each, newest first.

- **[batch-4 residual 1: wrong-NOUN-sense interior snaps — 33% unchanged]** (2026-07-13 measurement) — migration fixed cross-POS only; the wrong-noun class needs a targeted lever: LLM re-snap over suspect/flagged steps (cheap), or distinctive-token embed weighting. New subclass: SENSE-GAP ("no WordNet sense fits the intent", undertow→siphon) → sense-SET/vec endgame evidence. Report: docs/inbox/2026-07-13-batch4-pan-measurement.md.
- **[batch-4 residual 2: SEMANTIC impoverishment of multi-word steps — bad_head 21%]** — phrases are captured + displayed, but a snapped multi-word step's semantic identity is the bare head's synset ("tangled passage" IS "passage" to graph/judge); modifiers have no semantic representation, hop-logic breaks. Fix: hybrid syn+vec identity (head synset + FastText phrase-vector) for multi-word steps — the natural Block-2 completion, feeds Path Completion geometry. Same report.
- **[sense fan: multi-word steps should fall back to the HEAD lemma's inventory]** (2026-07-11, real-bundle verification of the fan wiring) — a multi-word step ("public identity display") has an empty phrase-level inventory, so its fan renders nothing; but its intended sense came from the HEAD lemma snap ("display"), whose inventory exists and whose alternatives would be meaningful ticks. Fix: `ensureSenseInventories` also fetches the head key; `_renderSenseFan` falls back phrase-key → head-key when the phrase inventory is empty (label the fan "senses of 'display'"). Small, frontend-only. (Source: fan-wiring fix d0b9a9f4 follow-on.)
- **[single-word-synset vehicle requirement drops the BEST creative vehicles — surfaced by Phase B]** Phase B generation (2026-06-14, AVOID steering ON, emotional topics) is proposing fresh, specific, named-technical-object vehicles — `wax cylinder`, `pressed flower` (for nostalgia), `supersaturation` (euphoria), `groundwater` (serenity) — i.e. EXACTLY the live-metaphor register the mode-collapse audit identified as "what LIVES" (verdigris/moraine/Potemkin-village class). But the pipeline **skips them: "no synset"** — multi-word phrases and rare single words don't snap to a WordNet synset, so they never become edges. The diversity nudge is working precisely well enough to expose the next bottleneck: the **single-word-WordNet-synset vehicle requirement filters out the most evocative candidates**. Candidate fixes (future): allow multi-word/phrase-level vehicles (phrase-keyed edges, no synset), or a fallback FastText-embedding vehicle representation when no synset exists, or snap multi-word heads to their head noun's synset while preserving the full phrase as the vehicle label. Tension with the metaphor-graph schema (synset-keyed edges) — needs a brainstorm before building. Watch the Phase B skip-rate in the run log to quantify the loss. (Source: 2026-06-14 Phase B launch log.)
- **[batch-infrastructure wheel reinvention — consolidate the pipeline's operational primitives]** Operator observation (2026-06-12, judge-harness instrumentation round): "we seem to have invented several kinds of wheel here. Logging, idempotency, jsonl output, heartbeat, buffer handling, caching, transient retries, 429 parsing and timed restart, argparsing... that's a lot of terrain. These are good wheels. But their implementation details are all different. It's tech debt." Each long-running script grew its own variant: enrichment (`--resume` + `checkpoint_*.json`), generation runner (`.attempted` sidecar + tripwire + cost brake), judge harness (content-addressed call cache + per-repeat atomic checkpoint + heartbeat + reset-time parse-and-wait), snap (`.tmp`+rename diagnostics), sweep (per-variation isolation), plus at least three flavours of session-limit/429 handling and several JSONL writers. Candidate fix = a small shared `data-pipeline/lib/batchkit` (or similar): checkpointed-JSONL writer, idempotent-resume helper, LLM call cache, session-limit wait/retry, heartbeat/timing logger, std argparse scaffold — adopted opportunistically as each script is next touched, NOT a big-bang rewrite. Adjacent: merge into (or promote alongside) the existing Backlog item "Pipeline Tooling Consolidation & Relevance Audit" at triage. (Source: 2026-06-12 operator capture, do-not-fix-now.)
- **[`import_familiarity.py` ↔ familiarity-xlsx drift blocks faithful clean rebuilds]** The SQLUNET clean rebuild (`import_raw.sh`) aborts at step 9: `import_familiarity.load_familiarity` does `headers.index("Dom_Pos"/"Freq_Dom"/"MultLex_Percent"/"EntryType")`, but the current `Full list GPT4 estimates familiarity and Multilex frequencies.xlsx` has headers `Word/GPT_Fam_dominant/GPT_Fam_probs/Multilex/Type/Subtype_MWE`. The function's *docstring* already references the new names — a half-finished migration: the familiarity source was swapped to a newer GPT version but the code's `.index()` lookups were never updated. Two consequences: (1) `import_raw.sh` can't complete as-is; (2) even after a column-rename fix, a rebuild would regenerate `frequencies.familiarity`/`rarity` from a *newer* source than production was tuned on → silent change to a tuned input. So a *faithful* clean rebuild needs a deliberate familiarity-versioning decision (reproduce the old file, or adopt the new one + re-tune + re-eval), not just a rename. This is the raw-source provenance rot the SQLUNET review flagged (now documented by `seed_sources`/`seed_meta`). The SQLUNET tables were landed **additively** instead — see Backlog "SQLUNET seed-source import". (Source: 2026-06-05 SQLUNET import — rebuild abort at step 9.)
- **[grading UI — filters + triage-score integration (don't delete garbage, filter it)]** Julian won't manually grade ~2.5k topic-vehicle pairs, so the grading tool needs to prioritise attention WITHOUT discarding low-quality chains. Three pieces: (1) **topic-dropdown filter `Graded | Ungraded | Both`** (concrete, small; uses existing verdict data so Julian can focus ungraded topics) — first to build; (2) **liveness/quality filter+sort** — attach the one-shot triage scores (`data-pipeline/grading/triage_scores.jsonl`, keyed by `chain_signature`) into the sidecar so the UI can sort chains by score and band-filter (e.g. hide <4, surface 7+) — garbage stays accessible, just deprioritised; (3) **contrastive-sampling helper** — per topic, surface a deliberate spread (top + middle + bottom by triage score) so each topic's ~3-5 grades capture live-vs-dead CONTRAST (see the ground-truth-coverage note). Needs: sidecar route to join triage scores to chains; mf-app grade-mode filter controls (TDD + the C2 no-reheat path). (Source: 2026-06-05 Julian — "add filters to the grading UI… topic dropdown should have Graded|Ungraded|Both".)
- **[generator feedback from the Forge-Reader triage — vehicle diversity + topic-class blind spots]** The 2026-06-05 one-shot liveness triage (876 handpicked chains, persona `data-pipeline/grading/liveness_judge_persona.md`) surfaced actionable generator signals, NOT just a score: (a) **vehicle over-reuse** — `fermentation` 20×/`undertow` 19×/`amber` 16×/`tide` 12×/`eclipse` 12× bolted onto many topics, mostly DEAD (mean ~4) → add the worst as soft anti-examples / a diversity penalty in the chain prompt, or cap per-vehicle reuse; (b) **big primary emotions die** (serenity/wrath/despair/gratitude/rapture/scandal — 0-1 hits/10) because the generator reaches for the culture's canonical dead metaphor (fire/storm/water/wound/ghost/hearth) → consider topic-class-aware prompting or accept these as low-yield; (c) **what LIVES** = named-technical-object & process vehicles (verdigris, moraine, Potemkin village, taxidermy, annealing, assay, escrow) on cognitive/social topics → could bias vehicle proposal toward specific instruments over elemental nouns. Uncalibrated (Opus judge, not Julian's grades) — treat as a hypothesis generator for the chain prompt, validate against real grades later. (Source: 2026-06-05 triage; `data-pipeline/grading/triage_notes.md`.)
- **[generation runner — cost-accounting over-charges on 429s + reused-Haiku (phantom est_cost)]** `generate_metaphor_edges.run()` charges the per-topic estimate at point-of-spend in a `try/finally`, so a topic that **429s** (session limit, actual cost $0) and a topic whose **Haiku is reused free** via `--haiku-jsonl` both still accrue full estimate. Safe DIRECTION for a spend brake (over-counts, never under) but `est_cost_usd` overstates real spend — the 2026-06-04 200-loop reported est $24 on ~$3–4 real (~$21 phantom from a 114-topic 429 storm). Risk: a long 429 outage on the multi-day 10k run could falsely approach `--max-cost-usd` and halt a run that's spending nothing. Fix options: have `claude_client` surface the CLI's `total_cost_usd` (it's in the result JSON, $0 on 429) and charge actual, OR skip the charge when the call raised a known-$0 error class, OR pass `haiku_cost_per_topic=0` when `--haiku-jsonl` is set. NOT urgent (caps are generous); pairs naturally with the SESSION-3 tripwire fix. (Source: 2026-06-04 200-loop post-mortem; tripwire false-trip fixed in `6b3cd03b`, this cost arm left as follow-up.)
- **[import SemCor tagcounts from sqlunet_master.db — fixes sense-selection AND POS-dominance properly]** The DB's missing per-sense usage frequency (the root of both the sense-snap blocker and the verb/function topic sneak-ins) IS available in SemCor, bundled in `data-pipeline/raw/sqlunet_master.db` (sqlunet = WordNet + SemCor tagcounts). Importing tagcounts → (a) deterministic dominant-sense selection that replaces most of the LLM disambiguation in `metaphor_disambiguate` (free, reproducible; LLM becomes a tie/zero-count fallback), (b) true POS-dominance filter catching the gerunds (browsing/voting) + balanced cases (thaw n3/v1, regard n7/v3) that the free `wn_noun_primary` sense-count ratio misses. Julian offered to upload `sqlunet_master.db`. The 10k disambiguation is HELD pending this. (Source: 2026-06-04 Julian — "if we import SemCor we get a better POS filter".) → **REVIEWED + PROMOTED 2026-06-05:** confirmed + broadened by the full SQLUNET data-strategy review (SemCor `tagcount`+`sensekey`+`sensenum`, plus `domains`, BNC, provenance). Decided scope + operator answers (import both SemCor+BNC; clean rebuild; grab sensekey) now in Backlog → "SQLUNET seed-source import". Review: `docs/inbox/2026-06-05-sqlunet-data-strategy-review.md`.
- **[`claude` CLI session limit — don't run concurrent LLM jobs; the 10k run must lean on resume]** Running the 200-loop generation + the disambiguation pass concurrently tripped the `claude` CLI 429 "session limit · resets 10am UTC" (33 topics errored, cost $0, retried cleanly on resume since they're not marked `.attempted`). Throughput is session-capped. Run LLM-heavy jobs serially; the multi-day 10k generation must be resumable (it is) and re-launched when the limit frees. (Source: 2026-06-04 200-loop run.)
- **[frequency-head topic selection — add a POS-dominance filter]** The 10k disambiguation head (`metaphor_disambiguate.head_lemmas`) selects high-frequency enriched-noun lemmas, but some are primarily verbs/function words whose only noun sense is rare (`down`→"feathers", `take`→"film take", validated 2026-06-04). The disambiguator picks the dominant *noun* sense correctly, but these are marginal metaphor topics. No POS-frequency data in the DB to filter them (same gap as sense-frequency — WordNet tagcounts never imported). Future refinement: require the lemma's dominant POS = noun (needs POS-frequency import, or a heuristic). For now the grading loop filters quality downstream; minority of topics. (Source: 2026-06-04 disambiguation validation.) → **ADDRESSED 2026-06-05:** the SemCor `tagcount` + BNC POS-freq import (Backlog → "SQLUNET seed-source import") supplies the POS-frequency data this item flags as missing — SemCor gives sense-precise POS dominance, BNC adds 58% more verb-dominant catches.
- **[generation runner — deferred cosmetic review items (pre-spend review 2026-06-04)]** The adversarial pre-spend review of `generate_metaphor_edges.py` + `metaphor_live_rate.py` raised 23 findings; all important/safety ones are fixed (cost-at-spend, in-loop cost cap, tripwire fed on zero-record batches, per-topic judge sampling, self-metaphor skip, within-topic signature dedup, idempotent `.attempted` resume sidecar, flush+fsync, abs_floor retune+calibration note). DEFERRED cosmetics: (1) **cross-file/cross-round `chain_signature` dedup** belongs in the grading consumer (`grading_sidecar/routes/chains.py` `get_chains` unions all `sonnet_chains_provisional_r*.jsonl` with no signature dedup) — a walk re-proposed in a later round shows as a duplicate grading card (partly mitigated by the round-3 prior-verdict prefill keyed on signature); (2) `metaphor_disambiguate.select_candidate_topics` does N+1 `candidate_senses` queries (measured ~1.3s for 7.5k lemmas — fine, YAGNI, no action); (3) consider expressing tripwire `min_judged`/`baseline_n` in topics rather than verdicts so the arming cost is legible in $ (now documented in `new_tripwire`). Also: add `*.attempted` to gitignore (runner resume sidecar alongside the chain JSONL). (Source: 2026-06-04 pre-spend review.)
- **[DB metaphor_judgments.label is v1; grading emits v2 verdicts — reconcile before JSONL→DB judgment ingestion]** `metaphor_graph.apply_schema` + `apply_graph_view` applied to the run `lexicon_v2.db` (2026-06-04, readiness for the deferred consolidation step; generation itself is JSONL-native). `graph_edges` now returns ~823k derived-arm rows + 0 metaphor edges (none until bridges are ingested AND judged `live`). Gotcha: the DB `metaphor_judgments.label` CHECK is the **v1** set (`live/dead_synonym/dead_lakoff/irrelevant/edge_case`), but the live grading tool now emits the **v2 two-axis** verdict (linkage good/bad + metaphor live/dead/irrelevant + tiers, keyed by `chain_signature`). When we consolidate JSONL judgements → DB, either migrate the `metaphor_judgments` schema to v2 or define a v2→v1 mapping. Tied to the unresolved edge-vs-path liveness fork. (Source: 2026-06-04 schema-apply.)
- **[metaphor-edge GENERATION phase — sense-accuracy is a HARD GATE before any scaled run]** Follow-on to the context-free-edge investigation: since edges must be LLM-generated, Julian wants continuous background generation of the `metaphor_bridges` layer (Haiku vehicles → Sonnet chains → `chain.v1` JSONL → judge) while the judge matures. Decisions (2026-06-04): hybrid-throttle + per-batch live-rate tripwire (proxy Haiku/Sonnet judge as the monitor, not the final gate); top few-k frequency-head first. **BLOCKER (run NOT launched):** `lexicon_v2.db` has NO sense-frequency data (WordNet SemCor tagcounts never imported), so auto sense-selection mis-picks common-word senses, sometimes embarrassingly (feel→"genital stimulation", house→"playing house", love→"sexual activities", fire→"firing weapons", must→"grape juice"). Mass-generating on these = garbage. This **is** the deferred "Snapping reconciliation + sense-accuracy" item, concretely demonstrated → it must be fixed first. Recommended path: (1) start the loop now on the ~200 already-curated correct-sense spike/grading topics; (2) build a one-time LLM sense-disambiguation pass (batched Haiku, ~$10-15) to extend correctly to the next few-k; (3) runner consumes whatever vetted topics file exists. BUILT: `data-pipeline/scripts/select_topics.py` (sense-noisy, not yet production-usable). NOT built: the continuous runner + the disambiguation pass. Pending Julian A/B call. Full handoff: `docs/inbox/2026-06-04-metaphor-edge-generation-handoff.md`. (Source: 2026-06-04.)
- **[context-free edge population — derivation REFUTED; the deliverable is the judge, not a derivation engine]** Investigation (2026-06-03, report at `docs/inbox/2026-06-03-context-free-edges/FINAL_REPORT.md`, ~$1.10 API spend) tested whether high-quality context-free metaphor edges can be DERIVED from the ~80k shared-feature enrichments instead of generated per-topic. **Answer: no, measured 5 ways and on human labels** — shared-features discriminate apt/inapt at AUC 0.55 & recall apt vehicles at 2% & are anti-correlated with aptness; embedding ANN bands anti-correlated (median apt vehicle rank 2,870/81k); WordNet `relations` (234,810 rows) recover 0/7 canonical live pairs; only 0.31% of an LLM's own claimed bridge-features exist in both concepts' enrichments. Root cause: cross-domain metaphor is the *low-surface-similarity* case, the opposite of any similarity substrate. **Generation is the only mechanism that works and is cheap** (~$0.06 Haiku + ~$0.19 Sonnet/topic → ~$20-29k for 100k, ~$6k Haiku-only, ~halved API-direct; ~$3-10k at the realistic ~15-35k queryable frontier — `frequencies` shows the curated-lemma universe is ~35k, not 100k). **Cost is NOT the binding constraint — quality admission is:** there is no automated live/dead judge in the codebase, and human ground truth is ~12-21 verdicts over 3 concrete topics (zero on the abstract-emotion core). **Recommended successor milestone: build + validate an automated live/dead judge calibrated to Julian's grading, and expand human grading across topic classes — i.e. scale the existing grading-tool bootstrap loop, query-weighted — IN PLACE OF any context-free-edge derivation work.** The derivation idea should be triaged **closed-negative**, not parked. NOTE for triage: the concreteness signal (AUC 0.81 on the spike cohort) INVERTS on human-graded concrete topics (AUC 0.375) — a Karpathy-Loop-1/G8 cohort artifact; do not ship it as a filter without per-topic-class human re-validation. (Source: 2026-06-03 ultracode orchestration.)
- **[per-hop linkage marking — localise Bad Linkage to the offending edge]** Grading `linkage` is a SINGLE verdict over the whole chain (`JudgementRecord`, keyed by `chain_signature`; docstring "are the path's edges accurate?" = collective over all edges). So a chain with one context-bound/leap hop and four good hops can only be marked bad as a whole — no way to attribute the failure to the specific edge (edge admission to the future `graph_edges` keys on `linkage=good`, so a single bad hop either poisons the admitted set or discards four good edges). Stopgap today: call the bad hop out in `notes` (free-text, low-friction; optional light convention e.g. `stillness→camouflage: context-bound`). Structural fix: a per-hop / per-bridge linkage axis (select an edge in the chain, mark IT) → true per-edge labels; this is the already-deferred "degrades after hop k" truncation generalised. BUT it forks on the unresolved **edge-vs-path liveness** question: does graph-completion consume per-edge labels (then per-hop marking is load-bearing and the current path-level grain under-delivers) or path-level chains (then current grain is right and per-hop is noise)? Brainstorm before building — tied to "what graph-completion actually looks like". (Source: 2026-06-03 grading session — context-bound hops `ambush→stillness→camouflage…`; corrects a mid-conversation mis-claim that the tool already supports per-hop marking.)
- **[grading prompt-quality clauses must reach the Stage A run branch]** The chain generator `run_chain_spike.py` (+ the W4 head-polarity clauses in `head_extraction_backfill.py`/`metaphor_graph_enrich_sonnet.py`, + the 2026-06-03 context-free-hop clause in `run_chain_spike.build_prompt`) live ONLY on `metaphor-graph/grading-rhs-affordances`. `metaphor-graph/enrich-stage-a` (the Stage A "4 proposers over 200-topic Loop 2 cohort" branch) is a PARALLEL branch with neither the generator nor any of these clauses. Before any Stage A proposer run, the grading lineage must flow into the Stage A branch (rebase enrich-stage-a onto the grading tip, or merge/cherry-pick the generator + clauses) — else the run regenerates the same context-bound / dead-head chains the clauses exist to prevent. Branch-consolidation decision for Julian (don't auto-merge). (Source: 2026-06-03.) → **RESOLVED 2026-06-04:** `metaphor-graph/enrich-stage-a` fast-forwarded to the grading tip (`6bd73963`) — the chain generator + all the head-polarity/context-free-hop clauses now ride the run branch.
- **[browse-staging redeploy vs the grading-live worktree]** `.worktrees/next` was repointed from `staging` → `grading-live` (2026-06-01) to home the grading sidecar there robustly. Two loose ends for the BROWSE staging site (also served from `metaforge-next` out of that worktree): (1) `deploy/staging/deploy.sh` does `git pull --ff-only`, but `grading-live` is local-only (no upstream) → that script now fails if run; (2) the browse Go API (`metaforge-api-staging`) still runs its pre-switch binary (browse works, but isn't rebuilt from grading-live). To fully unify metaforge-next on `grading-live`: push `grading-live` to a remote (give it an upstream) or adjust the staging deploy to not assume one, and rebuild/restart the Go API from grading-live. Low urgency — browse is unaffected until that API restarts, and `staging` is treated as expendable. (Source: 2026-06-01 sidecar-worktree hardening.)
- **[grading verdict-latest tie-break — unify on max-ts]** `mf-force-graph.latestVerdicts` uses max-ts-wins; `mf-app.priorVerdict` (feeding the form prefill + `supersedes_ts`) uses last-in-array-wins. They agree only while file/append order matches ts order — true under single-writer append + server-injected ts, but a hand-edited/out-of-order `judgements_provisional.jsonl` or cross-restart clock skew could make the edge colour and the panel prefill/supersedes disagree on the "current" verdict. Latent, not a live bug. Fix = share one canonical rule (max-ts) between both. (Source: 2026-06-01 grading-RHS adversarial review, correctness lens, low/med-conf.)
- **[`was-prior` marker vs lossy v1 coalescing]** The new `.was-prior` marker on the L/D/I buttons highlights **Irrelevant** for a legacy v1 `bad_path` record (`priorVerdict` coalesces null metaphor → `'irrelevant'`), falsely presenting it as the prior verdict. Pre-existing lossiness, but the marker amplifies it from a phrase to a highlighted button. Bounded: ~5 legacy v1 records, **not** in the new R1 `sonnet_v1` cohort (whose verdicts are real v2). Fix = only mark `was-prior` when the raw normalised metaphor is non-null (thread the uncoalesced value). (Source: 2026-06-01 grading-RHS adversarial review, data-safety lens, low/high-conf.)
- **[design-system adoption — deferred to the Browse-frontend milestone]** Claude Design handoff imported at `docs/design-system/` (unified two-theme token architecture + Parchment skin + a grade-mode spec). Triage 2026-06-01: the grade-mode spec **already converged** with what we shipped (two-axis verdict, tiers strong/ironic/surprising, tri-state Both/Ungraded/Graded filter, `›` node affordance, and its `chain`-vs-`path` split == our phrase-vs-head) → deferring is safe; nothing to tear up. Adopt when Browse frontend begins: (1) replace the drifted dark-only `web/src/styles/tokens.css` with the canonical `colors_and_type.css` architecture (structural `:root` + `[data-theme]` blocks + `color-mix()`-derived washes + the fuller token set — **near drop-in: the dark values are byte-identical**, so dark renders unchanged); (2) Parchment skin + theme toggle on the `mf-app` wrapper; (3) de-hardcode `mf-*` incl. the round-2 `label-layer`/`mf-force-graph`/`mf-grade-panel` (~35 colour literals) → semantic tokens; map our 3 tiers → `--colour-forge-strong/-ironic/-complex`; (4) type recipes (`.mf-word-hero` etc.) need a Shadow-DOM sharing strategy (custom props pierce shadow DOM, class recipes don't); (5) graph overlays in the spec not yet built — target rings (slate-blue) + explicit active-path-vs-trail. **One item with near-term bootstrap-loop value (optional, small schema touch):** promote grade issue-tags (`merge/padding/leap/other`) from notes-prefix to a structured `tags[]` field, so failure modes are filterable when building the Round 2 prompt. Reconcile on adoption: handoff tier is single-select; ours is multi-select (round-2 decision stands). (Source: 2026-06-01 design-handoff triage.)
- **[grade-graph scalability, before multi-topic/fog-of-war]** `mf-force-graph.buildGradeGraph` + `latestVerdicts` are recomputed from scratch on every access; `isNodeVisible` → `visibleGradeNodeIds` → `gradeLinks` getter rebuilds the whole graph **once per node per visibility pass** → ~O(N·E·J) + GC churn per filter toggle. Imperceptible at today's per-topic scope (~50 nodes); ~1.5M ops at a cohort-wide ~800-node graph. Fix = memoise `buildGradeGraph`/`latestVerdicts` against (gradeChains/judgements identity) in `willUpdate`/`updated`, and precompute `visibleGradeNodeIds` once per pass into a Set the predicate reads → collapses to O(N+E+J). Pre-exists the 2026-06-01 backlink work (which only added per-node backlink bookkeeping to the hot rebuild). Needs its own scoped refactor + regression tests for the C2 no-re-heat tri-state path. (Source: round-2 review, scalability lens, `important`/high-conf.)
- **[grade-graph tooltip DOM, same trigger]** Backlink tooltip DOM is built eagerly (not lazily on hover) for every visible node and rebuilt on each pathFilter toggle; row list is uncapped, so a high-degree hub node could hold hundreds of permanently-resident hidden row divs at fog-of-war scale. Fix = cap rendered rows (e.g. first 12 + "+N more"). Fine as-is per-topic; revisit with the memoisation item above. (Source: round-2 review, scalability lens, `minor`.)

## Backlog (no clear slot yet)

- **State of Metaforge survey — triaged follow-ups** *(2026-06-22 validate-first breadth survey; operator-triaged fix/defer/skip; full risk register + evidence in `docs/inbox/2026-06-22-state-of-metaforge.md`)* — the deep forge/judge thread is tracked in **Active**, not duplicated here. Runtime is healthy (frontend 4 gates green, pipeline 1016, sidecar 185); these are the surrounding fixes.
  - **Fix — UX (the daily mobile grading surface):**
    - Lift `grade-view-toggle` to a styled segmented control matching `.path-filter` (copy the bordered inline-flex block + aria-pressed); fixes mobile overflow — the 4 view buttons fit one row. [S/high]
    - Reduce grade-top density on mobile — conditionally omit topic picker + path filter in walk/regrade/sensecheck (not needed there); halves grade-top height and frees the 9rem padding-right concern. [M/high]
    - Fix walk-bar wrap on 375px (Prev/counter/"N left"/Next/✓/dwell/Skip wrap to a messy multi-line toolbar; the position readout + dwell text repeat the same count in two framings). [S/med]
    - Add loading indicators in grade mode (per-async `gradeLoading` @state + reuse `.loading-ring`/`.status-message`) — kills the blank-on-slow-network ambiguity. [S/med]
    - Return focus to a sensible element after verdict submit — topic view → chain-list/first card; walk view → `[data-testid=walk-next]`. [S/med]
    - Add a result/chain count + progress summary after topic load ("23 chains — 8 graded, 15 ungraded"); data already in gradeChains + verdictedSignatures; valuable on mobile with no graph overview. [S/med]
    - Add a keyboard-accessible visually-hidden `aria-live` summary for the 3D graph in browse mode (central node + synonym/hypernym/hyponym counts), fed from the same LookupResult prop. [M/med]
    - Replace right-click copy with a long-press / explicit copy button on mobile (the `navigator.clipboard` call is already there). [M/med]
    - Align mobile/desktop breakpoints to one value (`--mf-desktop-breakpoint`) so the 768px CSS and 900px JS thresholds stay in sync. [S/med]
    - Use `level='warn'` on the pending-verdicts banner (amber); keep default 'error' for auth/load failures — restores the designed semantic distinction. [S/low]
  - **Fix — backend:**
    - Update the cascade parity fixture for silence/velvet (crib `no_properties` → `scored` + numeric fields) to restore `internal/forge` green — **quick-fix only; if no quick fix, rip the test out** (project not mature enough to harden around DB state). [S/high]
    - Diagnose/fix the truth/hammer union miss (measure actual truth↔hammer cosine vs [0,2.0]; decide stale-claim vs coverage hole vs `t.Skip` with a pointer). [M/high]
    - Regenerate `lexicon_v2.sql` from the live DB — **schema-only (`--schema`) + a separate data-restore step** (the dump is ~780 MB and 9 tables behind). [S/med]
    - Add a `conftest.py` to `data-pipeline/scripts/` (sys.path.insert) so pytest runs from repo root without ModuleNotFoundError. [S/med]
    - DB per-row provenance `(model, prompt_variant, run_date)` so Go parity fixtures stop silently desyncing as enrichment accumulates — see also "Pipeline Architectural Review" Q3. [M/med]
  - **Fix — infra:**
    - Add a test-gate step to deploy scripts (`go test ./... || exit 1`, `npm test -- --run || exit 1` after build) — local safety net independent of CI. [S/med]
    - Write a repo-root Makefile (`make test`/`build`/`deploy`) unifying the three test invocations + documenting which venv per suite. [S/med]
    - Track/gitignore the stale `deploy/staging/Caddyfile.active` (per the `*.active` convention) or update it to current topology. [S/low]
    - Grading sidecar `deploy.sh`: poll-until-ready instead of `sleep 2` health-check. [S/low]
  - **Fix — repo hygiene:**
    - Merge `metaphor-graph/judge-harness` → main (12 unique; Stage-2 liveness harness, κ 0.332→0.335). [S/high]
    - Merge `metaphor-graph/completion-harness` → main (6 unique; learning-curve + path-geometry harness). [S/med]
    - Investigate + archive/delete `tmp-head-test` (21 unique; likely a duplicate mobile-walk UAT series). [S/med]
    - Archive fully-merged historic branches (spike/*, m02/*, m03/*, review/*, staging, workspace/eval-experiments, loop, loop-meta — 0 unique each). [S/med]
    - Add a branch-purpose note for `grading-data` ("LIVE DATA STREAM — do not merge or delete"). [S/low]
    - Remove the `.worktrees/qodo-triage` worktree (fully merged). [S/low]
    - **Delete `feat/umami-analytics` + `feat/steal-shamelessly` branches** (operator: kill them; cherry-pick the Umami script only if analytics still wanted). [M/low]
    - The judge-harness / completion-harness / tmp-head-test merges need a short **integration brainstorm** so none are stranded by a careless merge (operator-flagged).
  - **Fix — forge:**
    - Deploy the 3,125 Phase B stock chains (`chain-topics_stock.jsonl`, rescued at a23b0107) to `.worktrees/next` for grading — expands the gradeable corpus 1.3× with no new generation spend. [S/high]
    - Dedicated **Thesaurus validation + UX pass** — the survey covered the frontend build/test/e2e + entry-point components but not a Thesaurus-specific functional+UX deep-dive (operator flagged: "Thesaurus not evaluated?"). [S/med]
  - **Deferred (no slot yet):**
    - Code-split 3d-force-graph / Three.js (lazy-import on demand) — halves initial parse cost, clears the chunk-size warning. [M/med · frontend]
    - `lemma_embeddings` half-coverage (56,181 / 107,519 synsets) — sparse embedding candidates for rare/abstract lemmas (the truth/hammer miss is one symptom). [low · backend]
    - `test_export_chain_glosses.py` / `test_resnap_glossed_corpus.py` bare-import fragility — bundle with any future CI work. [low]
  - **Decided — skip / do not re-raise:** full GitHub Actions CI (deprioritised — single-dev, no users); ARIA for the grading tool (`mf-topic-picker`, focus-visible, search-bar combobox completion) — not required; 3D-graph keyboard/AT access — skip (architectural; the browse `aria-live` summary above is the lightweight substitute); Notes button across walk/regrade/sensecheck — skip.
  - **Already tracked in Active / Inbox (no new entry):** `metaphor_bridges` materialisation + LINKAGE-judge re-tune + Compositor (Blocks 2/3 + Downstream); phrase-as-node / single-word-synset vehicle skip (Block 2 + Inbox); emit-the-sense → main (**DONE — PR #21 merged 2026-06-23**); Liveness Re-measure (**DONE** — Active Block 1).
  - **Operator flags resolved:** gold sense-contamination — addressed (gloss-backfill 52%→78% + Liveness Re-measure); grading-app round-1 reconciliation — ✅ verified + moved to **Done** 2026-07-02 (all four deliverables confirmed on-branch).

- **Grading backend contracts + unification (tech debt)** *(captured 2026-06-17 from the grading-app divergence diagnosis)* — the grading instrument reinvented backend infra (auth, routes, persistence) as a Python FastAPI sidecar parallel to the Go forge API, serving Python-pipeline JSONL not the product's Go/SQLite store. Two graduated, deferred steps (justify-before-doing): (1) **shared contracts** — common types/schema between the Go API and the grading data, one canonical chain/edge store both read (the ChainStore from round 1 is the down-payment); (2) **backend unification** — the *judge* serving migrates into the Go product once the judge graduates from bootstrap instrument to product infrastructure (the forge will gate on the distilled judge). **Explicit non-goal: do NOT rewrite the Python grading sidecar to Go merely to cure the deploy smell** — code/data separation (round 1) is the cheap fix; a language rewrite is warranted only when the instrument's outputs become permanent product infra. The grading *capture* instrument can remain Python indefinitely as long as it shares contracts. Gated on: round-1 reconciliation; the judge's instrument→product graduation (judge-harness outcome).

- **SQLUNET seed-source import — SemCor + domains + BNC + provenance** *(LANDED additively 2026-06-05; clean rebuild deferred)* — Full data-strategy review of the complete SQLUNET source (117 tables) at [`docs/inbox/2026-06-05-sqlunet-data-strategy-review.md`](../inbox/2026-06-05-sqlunet-data-strategy-review.md) replaces the original undocumented "stab in the dark" import with a documented, revisitable standard. Gates the held ~$1.9k 10k generation run (sense-frequency was the blocker).
  - **✅ STATUS 2026-06-05 — LANDED in the working `lexicon_v2.db` via additive migration** (`data-pipeline/scripts/migrate_add_sqlunet_tables.py`, branch `metaphor-graph/sqlunet-import`): `sense_attributes` (sensekey + tagcount; 185,129 rows / 34,767 tagged / 18,536 = 53% of curated synsets covered), `domains` + `synsets.domainid` (100%), `bnc_frequencies` (26,980), `seed_sources`/`seed_meta`. Production `frequencies`/`enrichment` preserved EXACTLY; metaphor schema re-applied; 26 tests green, no regression. **The canonical clean rebuild (decision b) was attempted but BLOCKED** by a pre-existing `import_familiarity.py` ↔ xlsx drift (see Inbox) — and would have regenerated `frequencies` from a *newer* familiarity source than production was tuned on, so additive is the *more faithful* path. The clean-rebuild lineage (4 importers wired into `import_raw.sh` + `SCHEMA.sql`) is committed and ready for when the familiarity importer is fixed.
  - **Decided import set:**
  - **SemCor** — the dropped `senses.tagcount` + **`sensekey`** + `sensenum` → new sense-level table. The fix for the sense-accuracy blocker: 53% curated-frontier coverage, 91% unique-winner dominant-sense WSD on multi-sense tagged topic lemmas, + POS-dominance cull of 672 verb-dominant noun heads (say/take/get). LLM disambiguation stays as fallback for the 47% untagged tail.
  - **`domains`** + the dropped `synsets.domainid` — free, 100%-coverage, 45-class semantic-type label. **Guard: type/grouping infra for M05/Bridge/thesaurus, NOT a cascade aptness feature** (89% cross-domain background, no live/dead separation — consistent with the context-free-edges refutation).
  - **BNC POS-freq** (`bnc_bncs` → per-lemma noun/verb pivot) — companion to SemCor for the POS-dominance topic filter (catches 995 verb-dominant vs SemCor's 604; 58% SemCor-invisible).
  - **`sources` + `meta`** (11 rows) — provenance/licence table; closes the Architectural-Review provenance gap.
  - **Operator decisions (2026-06-05):** (a) import **both** SemCor + BNC for the POS filter; (b) land via **clean base-DB rebuild** — no users, no migration needed; (c) **grab `sensekey`** while importing SemCor.
  - **Parked (Tier-2 — import only when the consumer exists):** `lexrelations` antonym+derivation+pertainym subset (Bridge traversal edges; exclude the collocation noise), VerbNet FOL layer, gold `samples` example sentences, FrameNet schema frame-graph.
  - **Excluded (deliberate negative space):** FrameNet annotation corpus (234 MB / 10.2M rows / 0% spine-joinable — the biggest leave-out), PropBank (83% redundant with imported VerbNet, verb-only), Predicate Matrix + SemLink (verb-only, redundant), ILI + Wikidata (opaque/<10% topic coverage), morphology/pronunciation/casedwords/poses.
  - **Build (when promoted):** base-DB rebuild via `import_raw.sh` + new `import_semcor.py` / `import_domains.py` / `import_bnc.py` + `SCHEMA.sql` additions; TDD, atomic commits. Provided complete copy lives at `~/.local/share/metaforge/sqlunet.db` (note: the build's `data-pipeline/raw/sqlunet_master.db` is currently absent — symlink the provided copy in, or re-point `import_raw.sh`).

- **Domain-pair liveness structure — diagnostic on the 45-class domain labels** *(idea captured + triaged 2026-06-05; gated — do NOT open as a scoring experiment)* — Hypothesis (operator): although ~89% of metaphors are cross-domain regardless of live/dead (binary cross-domain is non-discriminative), there may be *within*-structure — a per-source-domain mapping `d → subset D′` of target domains that yields a step-change in liveness rate. The free 100%-coverage 45-class lexicographer-domain labels (see "SQLUNET seed-source import" above) are the candidate substrate. **Triage verdict: keep, but reframe + gate — this is NOT another geometric-aptness experiment.**
  - **Why it's sound (not vapour):** it's *categorical/contingency* structure, not a distance metric — a different class from the geometric/similarity aptness measured dead (context-free-edges, AUC ~0.55). Conceptual-metaphor theory (Lakoff) says mappings ARE domain-structured (TIME→MONEY, ARGUMENT→WAR; productive source domains journey/war/fire/light, productive targets emotion/time/ideas). It is the correct refinement of "cross-domain is non-discriminative": ask *which pairs*, not *whether* cross-domain.
  - **Binding risk = the project-wide wall, quantified:** 45×45 = 2,025 ordered domain-pair cells vs **13 human verdicts** (8 live / 1 dead / 1 bad_path, one judge); the 1,588 r2 chains are *proxy-judge* labelled and spread to <1/cell. Per-cell liveness is unestimable; ~100 cells would look "significant" by noise alone (multiple-comparisons / garden-of-forking-paths). A prior built off the unvalidated Haiku/Sonnet proxy judge risks learning the proxy's bias, not liveness. WordNet lexicographer domains are a *coarse proxy* for Lakoffian conceptual domains — a null could be a fidelity artifact. Does NOT get unstuck until the automated live/dead judge + scaled human grading land.
  - **How to run it (cheap, falsification-first, ~1-2h, near-zero spend — NOT a scorer):** (1) pre-register a COLLAPSE of the 45 classes to ~6-10 coarse super-groups to keep cells populated; (2) single aggregate test first — does liveness depend on (collapsed) domain-pair at all (mutual information / chi-square vs the 89% baseline)? if null → close; (3) only if the aggregate clears, drill to specific productive pairs and treat each as a *hypothesis for targeted (query-weighted) human grading*, not a discovery to ship; (4) re-run periodically as grading labels accumulate.
  - **Gated on:** (a) the SQLUNET `domains` import landing; (b) materially more live/dead labels (rides the grading bootstrap loop). Synergy: this is the *legitimate* path for the domain label to become aptness-relevant — distinct from binary domain-distance, which the import's review guard keeps out of the cascade. Do not wire a domain-pair feature into the cascade until the contingency signal survives on human labels.

- **Metaphor Grading Tool — bootstrap-loop instrument** *(spec'd 2026-05-30; plan-pending)* — single-user web UI integrated into the thesaurus app as a grading mode, deployed to **`metaforge-next.julianit.me`** (staging) via path-scoped routing within its existing Caddy block; `metaforge.julianit.me` (prod) graceful-degrades via `/api/grading/healthz` probe (toggle hidden when sidecar isn't reachable). Toggle button on `mf-app`; search bar becomes topic picker; force-graph renders the topic's chain constellation with shared-node dedup; click-to-select-path opens the grade panel. Captures `live` / `dead` / `bad_path` / `irrelevant` verdicts on Sonnet-generated chains with 3-step confidence + tag-chip notes; persists JSONL + cross-session design notes to `data-pipeline/grading/` (committed, `_provisional` filenames, auto-commit every 15 min). Auth via Caddy basic + sidecar `X-Grading-Secret` (fail-closed both ends). Drives the active-learning bootstrap loop: bad_path verdicts injected as anti-examples in next-round Sonnet prompt; iterates until Wilson-CI convergence diagnostic flattens or 8-round ceiling. Also the deliberate seed of the production Forge UI in edit mode. Spec: `docs/superpowers/specs/2026-05-30-metaphor-grading-tool-design.md`. Promote to Next when implementation starts.
- **Snapping reconciliation + sense-accuracy** *(DEFERRED 2026-05-29 — must return soon; CAP-snap-recon)* — The Go `/forge/suggest` endpoint and the Python eval harness sometimes snap the same topic to different synsets (a significant Karpathy-loop deviation source). Separately, polysemy means a lemma can have genuinely different senses; the current `lookup_primary_synset` heuristic (noun-preferred/least-polysemous) fixes coverage but ignores the per-topic `_gloss` and so does not solve sense-accuracy. The gloss (present in `spike_2_topics.json` and the phase2 dumps) is the lever for accurate, gloss-grounded disambiguation. Need a single deterministic snapper shared by Go + Python (or Go accepting a pre-resolved synset_id), gloss-based sense selection, and a re-evaluation of whether prior loop results shift once unified. Stage A handles this minimally (single-source the Python side, flag the cascade-score-sense caveat); full reconciliation is the follow-up. Operator flagged this must not slip — verify snapping is as tight as realistically possible. See memory `snapping-reconciliation-deferred`.
- **Sense-gloss-only metaphor enrichment variant** *(captured 2026-05-24 during the metaphor-enrichment spike brainstorm)* — option (c) from the input-granularity decision: feed the LLM only the sense definition, no word. Tests whether the model is metaphor-mapping the *concept* or pattern-matching the *word*. Deferred to backlog (budget-bound, not priority-bound) — promote to a small ablation experiment once the main spike has shipped and we have a reusable runner. Production-awkward (the word is what users type) but useful research signal for understanding whether future enrichment can ride on glosses alone (cheaper, less polysemy-coupled) or fundamentally needs the lexical anchor.
- **Snap-tuning research** — see project memories `project_metaforge_snap_threshold_curve` and `project_metaforge_signal_weighted_snap_JSJSJS`
  - ~~Threshold default change 0.70 → 0.48~~ — **promoted into M02 — Asymmetric Ortony Scoring S04-D (in progress 2026-05-15)**.
  - ~~Curated vocab additions for sensorimotor losses (`resonant`, `earthy`, `angular`, etc.)~~ — **promoted into M02 — Asymmetric Ortony Scoring S04-G (queued, runs only if S04-D is partial)**.
  - Per-property signal eval extension as a closed-loop instrument *(still backlog)*
  - JSJSJS — signal-weighted snap (Stage 3 picks highest-aptness target, not highest-cosine) *(still backlog)*
- **Purge stale DB backups** — `data-pipeline/output/lexicon_v2.db.*-backup` (and `*.pre-purge-*` snapshots) accumulate during destructive operations like the M02-S04 retro and the 2026-05-17 pre-enrichment clean. Each is ~336 MB. They're gitignored so they don't bloat the repo, but they do bloat the worktree volume. Define a retention policy (e.g. keep newest two, archive older to `~/.local/share/metaforge/backups/`, prune anything >30 days unless tagged) and a small `scripts/prune_db_backups.sh` to enforce it. **Required: honour a `<dbname>.keep-for-<reason>` sentinel companion file as a "do not delete" marker** — M03's Stage-1 eval depends on `lexicon_v2.db.pre-purge-20260517` (tagged with `.keep-for-m03-baseline`) and must survive any pruning sweep. Run manually for now; promote to a periodic hook if it becomes a recurring chore.
- **Pre-existing Go handler test failures** — 8 tests in `api/internal/handler/handler_test.go` failing because the test fixture DB isn't being provided. Confirmed pre-existing at the pre-M01 main HEAD. Worth tackling alongside or just before the M01 review-loop since the reviewer will trip on these.

- **FU-LATENCY-BUDGET — Union-mode latency regression** *(captured 2026-05-25 during Karpathy loop preflight)* — `TestCascadeUnion_LatencyBudget` asserts union-mode `anger` limit=50 completes in ≤750ms; current measurement is ~5s against the live DB (regressed somewhere since the budget was set in M04). Confirmed pre-existing on pristine main, independent of the `getSynsetRowsBatch` chunked LEFT JOIN fix landed 2026-05-25. Slow path is concreteness gate + embedding scan at TopK=100, NOT the batch fetch. Test is currently `t.Skip()`'d with a pointer back to this entry so the Karpathy loop's wrapper test gate doesn't get blocked by it. Restore the test and investigate the gate / embedding-scan latency once a slot opens — likely 1-2h of profile-and-tune work.
- **Sweep-with-next-touch micro-fixes** *(from M03-pre-M04-deferrals review log, 2026-05-22)*:
  - **R1-D5** — `GetLemmaForSynset` (`api/internal/db/db.go`) returns bare `err` without wrapping. One-line `fmt.Errorf("GetLemmaForSynset failed for %s: %w", synsetID, err)` fix; pick up on the next `db.go` touch (likely the M04 cluster-prop work).
- **M04 follow-up — secondary-sense ANN candidate generation** *(captured during M04 brainstorm 2026-05-23)*: M04 v1 ships the ANN candidate path against a *single* polysemy-ASC primary source synset per lemma (mirroring the existing `resolvePrimarySynset` parity rule). For polysemous lemmas where a secondary sense would surface a better cross-domain vehicle, M04 v1 relies on the cluster-overlap path to catch it. **Open question for v2:** would running the ANN query against ALL source senses (5× cosine-scan cost on a polysemous lemma) surface MUNCH-apt vehicles the primary-only path misses? **Eval-first follow-up:** (a) extend the Python `evaluate_cascade_pair` harness to score all-senses-ANN vs primary-only-ANN candidate sets on the MUNCH-apt cohort; (b) if Python eval shows ≥5% lift on apt-pair coverage with acceptable inapt-pair noise, port the all-senses path to Go behind a config flag; (c) sweep cosine-scan cost vs candidate-quality on the production DB. Defer to post-M04 (post-Bridge maybe) so M04 ships its core lift without scope creep.
- **Atomic-commit hygiene — recurring pattern** *(R1-D6 + R4-D2 informational; M03-pre-M04 review log)*: commits `ccfa6c3b` and `218fad1d` each bundled 3-4 logically independent fixes (test + behaviour change + config plumbing). Project standard "Commit after each green test. Small, atomic commits. Never batch up changes" was violated twice in the same loop. Cannot fix retroactively (force-push trade-off). Watch-list item for future review-loop fix dispatches — each finding's fix should land as its own commit. Round-5 commit `b1f820cf` showed the discipline can be restored.
- **CI/CD pipeline** — referenced in MVP punch list, no dedicated milestone yet. **DEPRIORITISED (operator, 2026-06-22):** single-dev project, no active users → a full CI/CD pipeline is overkill. The cheap local substitute (deploy-script test-gate + repo-root Makefile) lives in "State of Metaforge survey — triaged follow-ups". Revisit when there are external users/contributors.
- ~~**20k-word enrichment** — 8k top-up *in progress as a side-task of M02 — Asymmetric Ortony Scoring S04* (running 2026-05-15, ~52h ETA, ~144 synsets/hour at batch-size 10). Brings DB from ~12k → ~20k enriched synsets. After import (`enrich.sh --from-json`), feeds S04-F re-sweep.~~ — **DONE** *(confirmed 2026-05-25 — ETA ~2026-05-17 elapsed; current DB reflects the full enriched state)*

- **Pipeline Tooling Consolidation & Relevance Audit** *(programme-level refactor; queued for after M02 — Asymmetric Ortony Scoring lands)* — captures portability/maintainability work surfaced during the M02-S04 retro. Two sub-goals:
  1. **Backfill four items** into the canonical production code:
     a. Move `BATCH_PROMPT_V2_SM` (sensorimotor prompt) into `data-pipeline/scripts/enrich_properties.py` alongside `BATCH_PROMPT_V2`, with a `--prompt-variant {physical,sensorimotor}` CLI flag. Currently lives in a test-script file (`m02_s04_test_sensorimotor_prompt.py`) which is brittle.
     b. Atomic incremental JSON writes in production `enrich_properties.py` (flush after every batch, .tmp-rename pattern). The 2525 Sonnet synsets lost when the in-flight broad run was killed on 2026-05-15 are evidence this matters.
     c. `--clear-existing` flag on the import path that DELETEs old rows before INSERT, instead of INSERT OR IGNORE silently keeping stale data. Useful for model switches and prompt iteration.
     d. Haiku-friendly worked-example IDs (numeric, not `oewn-foo-n`) plus explicit *"use the input ID verbatim"* instruction in the canonical prompt. Improves cross-model reliability — Haiku 39%-failed at ID format until this was patched in the local SM prompt.
  2. **Relevance audit** of existing pipeline tooling — which scripts/wrappers are now obfuscation rather than abstraction?
     * `data-pipeline/enrich.sh` — orchestrator wrapper for restore → enrich → pipeline → dump. In recent work we bypassed it entirely (called `enrich_properties.py` and `enrich_pipeline.run_pipeline` directly) because its assumption of restoring from `PRE_ENRICH.sql` doesn't fit incremental top-ups. Decide: keep + fix, simplify into a thin orchestrator, or retire.
     * `data-pipeline/scripts/m02_s04_*.py` — eleven ad-hoc scripts written during the retro. Triage: archive (audit one-offs that document the retro), formalise (the patch/import workflow patterns), or delete (superseded by formal versions).
     * Other potentially-defunct files: `evolve_prompts.py`, `evolve_trials.sh`, `ab_test_purpose_prompt.py`, `prompt_templates.py` — pre-M01 evolutionary-prompt-search era. Confirm whether any still active.
  - Goal: keep `code-as-documentation` of valuable patterns; remove clutter that misleads future contributors.
  - Cost estimate: ~1-day PR for the backfills + relevance audit doc.

- **`metaphor` package extraction & architectural cleanup** *(programme-level; queued for the M04 → Bridge gap)* — extract the shared language structure (concept-senses as nodes, semantic relations as edges, concreteness gradient, type-aware features, cascade scorer, candidate generators) into a small `metaphor` package so Forge and Bridge sit on top as thin orchestrators. See M04 roadmap doc's "language-structure framing" section for the structure-vs-orchestrator argument. Carries the following deferrals:
  - **From the M03-S05 review log (2026-05-21):**
    - **D1** — Tagged-union refactor for `CascadeResult` (`(Status, *Scored)` shape).
    - **D2** — Constrained-type discipline for `CascadeStatus` + `Composition` (Valid() methods / constructors).
    - **D3** — `CascadeCache` encapsulation (unexport maps, add accessors) — pairs with the package extraction.
    - **D4** — `Match` struct legacy/cascade split (or `GatePassed` → `*bool`) for cleaner JSON wire format.
    - **D5** — Nil-cache defence in `handleSuggestCascade` (lands when cache lifecycle moves into the package).
    - **D14** — Port Python `__post_init__` validation into `CascadeConfig.Validate()`.
  - **From the M03-pre-M04-deferrals review log (2026-05-22):** the observe-surface redesign cluster — three deferrals converged here independently:
    - **R1-D1** — `observe.Start` allocates a closure literal + variadic slice on every disabled-path call. Architectural fix is a typed `Timer` struct (or two-method `Stop()` + `StopWith(...)` API) so callers opt into variadic boxing only on the enabled path.
    - **R1-D2** — `handler_legacy_embedding_error_test.go` bypasses `NewHandlerWithCascade` via direct `&Handler{}` construction. Add a `newHandlerForTest(...)` constructor as part of the Handler-construction discipline rework alongside D3/D5; migrate the test.
    - **R1-D3** — `observe.enabled` global `atomic.Bool`; race vector under `t.Parallel()`. The observe surface redesign removes the global by injecting `*Timer` through the handler graph.
  - Cost estimate: ~1-2 day refactor for the core extraction; the observe-surface redesign is ~half a day on top — total ~1.5-2.5 days. Lands between M04 and The Bridge so the Bridge inherits the package shape on day one.

- **Pipeline Architectural Review** *(programme-level; queued after the tooling consolidation chunk above)* — design-level retro on how Metaforge maintains its three data tiers and the schema that holds them. Four lifecycle questions:
  1. **Schema change management.** `SCHEMA.sql` is the canonical DDL but it has drifted from the committed `lexicon_v2.sql` (which is the actual data dump). When a column is added (e.g. `synset_properties.salience` in M01), how does that propagate to (a) fresh-from-PRE_ENRICH DB rebuilds, (b) in-place schema upgrades on the live DB, (c) backwards compatibility for old enrichment JSONs? Today this is implicit and breaks when assumed (see M02-S04 DB-freshness incident on 2026-05-12).
  2. **Seed data lifecycle.** Raw sources (OEWN/sqlunet, SUBTLEX-UK, Brysbaert, SyntagNet, VerbNet, FastText) live outside the repo in `~/.local/share/metaforge/`. Provenance, versioning, and update cadence are undocumented. What's the story for "the FastText vectors have improved, refresh"?
  3. **Enrichment data lifecycle.** `synset_properties` and friends accumulate from many model/prompt runs over time. Today INSERT OR IGNORE silently mixes them. The clear-and-import pattern (from chunk A) fixes one symptom but the deeper question is: should the DB carry a per-row `(model, prompt_variant, run_date)` provenance, so we can roll forward/back and reason about which data was used in any given M0X eval?
  4. **Derived curation lifecycle.** `synset_properties_curated`, `property_vocab_curated`, `vocab_clusters`, `property_antonyms` are all rebuild-from-scratch outputs of the post-enrichment pipeline. Their build cost is significant (~30-60 min per full rebuild). Is there value in incremental rebuilds for surgical changes, or is the rebuild-everything pattern correct because the derived state is small relative to source state?
  - Output: an `ARCH-REVIEW.md` doc with recommendations, possibly spawning concrete follow-on milestones.
  - Cost estimate: ~half-day design doc, half-day to scope concrete follow-ups.

## Done (newest first)

- **Grading-app reconciliation (round 1 — unblock labelling)** *(verified landed 2026-07-02; operator marked obsolete/landed 2026-06-22, subsumed by the merged integration tip PR [#21](https://github.com/snailuj/metaforge/pull/21))* — all four deliverables confirmed on-branch: ChainStore fall-through reader (tested, wired into walk/chains/stats/sense_check routes), cohort renames to the `chain-topics_*` scheme, sense-checker skip button, deploy code/data separation (`grading-code` + `grading-data` branches). North star (grading IS Metaforge in `editor` mode) banked in the **Grading frontend unification** item, which remains in Next.
- **Farm-out model bake-off** *(2026-07-02, spike — full report `docs/inbox/2026-07-02-farmout-model-bakeoff.md`)* — six-arm liveness verdict: no cheap drop-in for Sonnet's chain-expansion; liveness tracks model capability + Claude-family bonus; Flash≈GLM; two viable paths (keep Sonnet / Flash+overgenerate+judge-filter). Shipped reusable infra: provider-agnostic `lib/openai_client.py`, bake-off harness (`bakeoff.py`, per-chain checkpointed judging), `--provider/--base-url/--reasoning-off` generation flags. Also closed out the emit-shape Stock Run (3,158 glossed chains committed).
- **Karpathy Loop 1 — Cascade Improvement Loop** *(13 iterations, paused 2026-05-25; lives on `loop` branch, not yet merged to `main`)* — open-ended fresh-subagent-per-iteration loop on top of the Phase 2 cohort + Lakoff cohort dual gate. **Headline: Phase 2 bootstrap median 2.3512 → 3.0264 (+28.7%)** with Lakoff held flat at 0.6000 across every iteration. 6 committed wins, 7 informative reverts. Key wins: WordNet lemmatiser fallback (iter 1, FU-2), noun-POS preference in lemma resolution (iter 2 op-promoted, +12.1%), `rerank_exponent=0.75` (iter 4), `d_cap` tuning to 0.68 (iters 5-6), concreteness-magnitude bonus (iter 10 op-promoted, +2.5%). **The loop's biggest finding**: Phase 2 and Lakoff cohorts have structurally inverted discriminator profiles — Phase 2 inapt vehicles are LOWER-concreteness than apt (intuitive); Lakoff inapt vehicles are HIGHER-concreteness than apt by design (anger→umbrella). Any global cascade tweak that lifts one cohort tends to hurt the other; the commit gate enforces this discipline mechanically and the reverted iterations are correct rejections of asymmetric moves. Full scoreboard, cohort diagnostics, plateau map, and loop-2 design notes at [`docs/inbox/2026-05-25-karpathy-loop-1-findings.md`](../inbox/2026-05-25-karpathy-loop-1-findings.md). Infrastructure that landed (orchestrator wrapper, immutable harness module, worktree isolation, chmod-locked immutables, Go deadlock fix on `getSynsetRowsBatch`) lives on `loop` branch — worth merging to main as preflight for any loop-2 round. Cost estimate: ~$10-15 in Anthropic credits for the +28.7% lift.
- **Metaphor-Enrichment Pivot Spike** *(merged to `main` 2026-05-25)* — three-phase exploration of LLM-generated metaphor enrichment with structured shared-feature rationale. **Verdict: PASS — promote to follow-on milestone selection.** Phase 1a (5 topics × dual model × 2 prompts = 20 calls, $0.28) validated format gates clean on both Haiku 4.5 and Sonnet 4.6. Phase 1b (20 topics × Haiku-only × 2 prompts = 40 calls, $0.41) introduced the **Sonnet-as-prompt-engineer** pattern — Sonnet wrote 3 gold few-shot examples (love/knowledge/fear, outside test set) baked into Haiku's prompts at template-load time. 63% cost saving vs dual-model. Phase 2 (200 topics × Haiku-only × 2 prompts = 400 calls, ~$2) delivered the M05-evidence cohort: **2.78× end-to-end discrimination ratio**, well above the 1.5× bail floor. The scored-pair separation_score remained near zero (+0.0055) — this turned out to be a measurement artefact (FU-1 metric correction now in `evaluate_loop_metric.py`). Per-`inapt_reason_type` discrimination at scale: `synonym_or_hypernym` 100%, `single_dimension` 62%, `same_domain` 55%, `dead_metaphor` 38% (hardest mode). Resolves all three M05 verdict caveats (n=1 inapt magnitude, aptness_rate=0, ~24% curator apt resolution). **Spike doc:** [`docs/inbox/2026-05-24-metaphor-enrichment-pivot-spike.md`](../inbox/2026-05-24-metaphor-enrichment-pivot-spike.md). **Findings:** [`docs/inbox/2026-05-25-metaphor-spike-findings.md`](../inbox/2026-05-25-metaphor-spike-findings.md).
- **M05 — Type-Aligned Structural Matching** *(merged to `main` 2026-05-24 at `8b3777ac`, `--no-ff` so M05 boundary survives in history)* — type-diversity bonus γ ratified at 1.0 after Phase 2 sweep showed monotone γ→separation_score signal (Δsep/Δγ ≈ +0.263 linear, confirming additive scoring math). Cohort diagnostic ([`m05_cohort_diagnose.py`](../../data-pipeline/scripts/m05_cohort_diagnose.py)) confirmed 100% preflight-clean — every drop is real candidate-gen rejection, not data hole. Verdict: [`m05_lakoff_gamma_verdict.md`](../../data-pipeline/sweeps/m05_lakoff_gamma_verdict.md). Three caveats survive into post-merge work: (1) n=1 inapt magnitude sensitivity, (2) aptness_rate=0 (γ moves ranks not absolute scores — calibration gap), (3) ~24% apt resolution against curator cohort (the harness measures curator-recall, not production quality — see the metaphor-enrichment pivot spike under Next for the cohort/recalibration response). 51 commits brought in; Go suite + Python 712 tests all green post-merge.
- **M03-S05 — Forge integration into Go API** *(landed 2026-05-21, branch `m03/cascade-gate-and-rank`)* — cascade scoring wired into `api/internal/forge` + `api/internal/handler` behind `--cascade` flag / `METAFORGE_FORGE_CASCADE=1` env var. Per-request hot path: 2 DB queries + N in-memory map lookups via `CascadeCache` (~50 MB at startup, eliminates ~4× per-candidate DB hops). SQL gate-pushdown via `shared_gated` CTE keeps query under 2 s on broad-coverage lemmas (vs 200 s without the CTE refactor). Scoring-math parity with Python verified to ±1e-6 by `cascade_parity_test.go` against the 4 scored crib pairs (anger→fire, idea→light, time→money, truth→hammer). Crib: [`2026-05-21-m03-s05-smoke-test-crib.md`](../plans/2026-05-21-m03-s05-smoke-test-crib.md); plan: [`2026-05-21-m03-s05-forge-integration.md`](../plans/2026-05-21-m03-s05-forge-integration.md). Surfaced finding: classical cross-domain metaphor pairs share no curated cluster between primary synsets, so the Go endpoint can't surface them today — broadening the candidate set via ANN over `synset_centroids` is the **M04 — Cosine-Sim Candidate Generation** milestone.
- **M02 — Asymmetric Ortony Scoring** *(closed empirically negative 2026-05-16)* — built three asymmetric scoring variants (`ortony_vehicle_salience`, `ortony_imbalance`, `ortony_log_ratio`) and exercised them via the M01 eval harness. The S04 retro identified a cohort-shape mismatch confound that was producing artifactual signal on the original sweeps. After the Haiku+sensorimotor rebuild balanced the cohort, **no scoring formula in the pointwise-property-overlap family beats the random_uniform null reference**. M02's algorithmic premise is empirically refuted. What M02 *did* deliver: a trustworthy eval harness on a balanced cohort, the `physical → sensorimotor` prompt rename (5.4 vs 0.8 sensorimotor props per synset), Haiku adopted as production enrichment model, and a cohort-shape diagnostic methodology (S04-A/B) that is now standard eval-harness toolkit. Detail: [`M02-S04-CLOSING-findings.md`](../../data-pipeline/sweeps/M02-S04-CLOSING-findings.md), [`M02-ortony-scoring-roadmap.md`](M02-ortony-scoring-roadmap.md).
- **Code-review-loop on M01 + snap memory-opt refactor** *(PR [#17](https://github.com/snailuj/metaforge/pull/17) — merged 2026-05-12)* — Holistic 4-round oscillating review (pr-review-toolkit ×3, superpowers, standards). 29 fix commits, 23 new tests (suite 512 → 535), 16 active deferrals captured. Round 4 CLEAN halt. Detail: `docs/superpowers/review-logs/2026-05-08-review-m01-and-snap-memopt-review.md`.
- **M01 — Automated Eval Harness** *(merged 2026-05-03)* — discriminative aptness evaluator, parameter sweep harness, MUNCH preprocessor, scoring-fn registry, baseline + sensitivity sweep configs, `SENSITIVITY-V2-FINDINGS.md`. S01 V2 Foundation + Aptness Evaluator, S02 Parameter Sweep Harness, S03 Baseline and Sensitivity Validation all delivered. ([roadmap](M01-eval-harness-roadmap.md), [context](M01-eval-harness-context.md))
- **Sprint Zero** — Backend API, data pipeline foundations, staging deployment.

## Conventions

- **This file is the single funnel** for all forward work. The whole lifecycle lives here: **Inbox → Backlog → Queued → Next → Active → Done**. There is no separate captures file and no parallel backlog. Capture raw ideas into **Inbox** with zero friction; everything else flows downward through triage.
- **Capture vs decision vs issue:**
  - Raw idea/observation/limitation surfaced mid-work → **Inbox** (here).
  - Settled architectural/convention choice → `docs/decisions/log.md` (append-only).
  - Externally-facing work only (licensing, public infra, community-visible) → GitHub issue. Internal engineering work does **not** go to issues — it lives in Backlog.
  - Findings / working notes / spike reports → dated `docs/inbox/*.md` docs (these are reference material, not backlog items).
- **Promotion moves text verbatim** (per the verbatim-copy standard) and deletes the source line, so nothing is duplicated across tiers.
- **Next is always the immediate next job.** It can be a milestone, a code-review-loop on a recently-merged milestone, a tooling task, a pre-flight blocker — whatever genuinely comes first. Do not assume Next must be a milestone.
- New milestones land in **Queued** with at minimum: name, why, depends-on, detail-doc link.
- Move to **Next** when its prerequisites are met (M-1 done, blocking tasks resolved, etc.).
- Move to **Active** when work starts; flesh out detail doc; create per-slice sub-docs as needed.
- Move to **Done** with a one-line summary and merge date when shipped.
- **Backlog** items have no current slot — items either lack prerequisites, are speculative, or are awaiting prioritisation. Promote to Queued (or Next directly) when a slot opens up. Adding to Backlog should never strand work that's actually ready to go.
- Detail docs live as flat `docs/roadmap/M0X-name-{roadmap,context,S0Y-name}.md`; if a milestone grows enough sub-docs to clutter, switch to a per-milestone subdirectory.
